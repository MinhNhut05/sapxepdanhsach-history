---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - package.json
  - package-lock.json
  - tsconfig.json
  - next-env.d.ts
  - next.config.ts
  - eslint.config.mjs
  - postcss.config.mjs
  - vitest.config.ts
  - src/test/setup.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - tests/smoke/homepage.test.tsx
autonomous: true
requirements:
  - IMPT-01
user_setup: []
must_haves:
  truths:
    - "The repository runs as a Next.js App Router application with a Vietnamese-first upload homepage."
    - "The codebase exposes reusable lint, typecheck, build, and Vitest commands for later phase work."
    - "Phase 1 execution starts from a minimal but real UI shell instead of placeholder docs-only artifacts."
  artifacts:
    - path: package.json
      provides: "Application scripts and dependency manifest for the Phase 1 stack"
      contains: "\"next\":"
    - path: src/app/page.tsx
      provides: "Initial upload-focused homepage"
      min_lines: 20
    - path: tests/smoke/homepage.test.tsx
      provides: "Smoke coverage for the initial page shell"
      contains: "ExamRoomAllocator"
  key_links:
    - from: src/app/page.tsx
      to: src/app/layout.tsx
      via: "App Router page/layout composition"
      pattern: "export default function"
    - from: package.json
      to: vitest.config.ts
      via: "test script points at Vitest"
      pattern: "\"test\""
---

<objective>
Bootstrap the repository into a runnable Next.js 16 application and lay down the execution shell for the roster import workflow.

Purpose: Every later Phase 1 plan depends on a real app, stable scripts, and a homepage that anchors the upload experience.
Output: Buildable project configuration, global app shell, test setup, and a smoke test for the homepage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the project toolchain and dependency manifest</name>
  <files>.gitignore, package.json, package-lock.json, tsconfig.json, next-env.d.ts, next.config.ts, eslint.config.mjs, postcss.config.mjs, vitest.config.ts</files>
  <read_first>CLAUDE.md, .planning/PROJECT.md, package.json, tsconfig.json, next.config.ts, eslint.config.mjs, postcss.config.mjs, vitest.config.ts</read_first>
  <action>Create a root-level Next.js 16.2.2 App Router setup that uses npm, TypeScript, and Tailwind CSS 4.2.2 in-place without wiping the existing `.planning/` directory. Set `package.json` scripts to exactly `dev`, `build`, `start`, `lint`, `typecheck`, and `test`. Install runtime dependencies for `next`, `react`, `react-dom`, `exceljs`, and `zod`. Install dev dependencies for `typescript`, `@types/node`, `@types/react`, `@types/react-dom`, `eslint`, `eslint-config-next`, `tailwindcss`, `@tailwindcss/postcss`, `vitest`, `jsdom`, `@testing-library/react`, and `@testing-library/jest-dom`. Configure `tsconfig.json` to use the `@/*` path alias rooted at `src/*`, keep `strict: true`, and include `next-env.d.ts`. Add `.gitignore` entries for `.next`, `node_modules`, `.env`, and `coverage`.</action>
  <verify>npm run lint && npm run typecheck</verify>
  <acceptance_criteria>
    - package.json contains the scripts `dev`, `build`, `start`, `lint`, `typecheck`, and `test`
    - tsconfig.json contains `"strict": true` and `"@/*"`
    - .gitignore contains `.next` and `node_modules`
    - vitest.config.ts exists and references a `jsdom` test environment
  </acceptance_criteria>
  <done>The repo can install dependencies and run baseline lint/typecheck commands with the expected scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Build the base Vietnamese-first app shell</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-data-foundation/01-CONTEXT.md, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</read_first>
  <action>Create `src/app/layout.tsx` with `lang="vi"`, metadata title `ExamRoomAllocator`, and a body wrapper ready for internal-tool screens. Create `src/app/page.tsx` as a server component that renders a single upload workspace with a heading, a short `.xlsx` import description, and empty slots for later upload/preview components. Create `src/app/globals.css` with Tailwind import plus a restrained internal-tool visual system: warm neutral background, readable spacing, and classes for cards, labels, and status badges that Phase 1 UI components can reuse.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - src/app/layout.tsx contains `lang=\"vi\"`
    - src/app/page.tsx contains `ExamRoomAllocator` and `.xlsx`
    - src/app/globals.css contains reusable selectors for cards and status badges
  </acceptance_criteria>
  <done>The homepage renders as a real upload-oriented shell instead of a blank starter page.</done>
</task>

<task type="auto">
  <name>Task 3: Add shared test bootstrap and homepage smoke coverage</name>
  <files>src/test/setup.ts, tests/smoke/homepage.test.tsx</files>
  <read_first>vitest.config.ts, src/app/page.tsx, src/test/setup.ts, tests/smoke/homepage.test.tsx</read_first>
  <action>Create `src/test/setup.ts` that imports `@testing-library/jest-dom/vitest` and any global cleanup hooks needed by Vitest. Add `tests/smoke/homepage.test.tsx` that renders the homepage component, asserts the presence of the Vietnamese upload heading, and verifies the `.xlsx` helper text is visible. Wire Vitest to load the setup file before tests execute.</action>
  <verify>npm run test -- tests/smoke/homepage.test.tsx</verify>
  <acceptance_criteria>
    - src/test/setup.ts contains `@testing-library/jest-dom/vitest`
    - tests/smoke/homepage.test.tsx contains `render(` and `ExamRoomAllocator`
    - vitest.config.ts references `src/test/setup.ts`
  </acceptance_criteria>
  <done>The repository has one passing UI smoke test that proves the app shell is executable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint passes
- [ ] npm run typecheck passes
- [ ] npm run build passes
- [ ] npm run test -- tests/smoke/homepage.test.tsx passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No starter-page placeholders remain in `src/app/page.tsx`
- The repository is ready for domain and API work in later Phase 1 plans
</success_criteria>

<threat_model>
<trust_boundaries>
- Browser request -> Next.js App Router page shell
- Local repository -> npm dependency/runtime configuration
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>01-01-T1</threat_id>
    <category>Tampering</category>
    <component>package.json and toolchain config</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Define the exact scripts used by later plans (`lint`, `typecheck`, `build`, `test`) and keep the dependency list limited to the approved stack from `CLAUDE.md` so later execution does not drift into ad-hoc tooling.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>01-01-T2</threat_id>
    <category>Information Disclosure</category>
    <component>Initial homepage shell</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Render only product copy and placeholders in `src/app/page.tsx`; do not hardcode sample student data, stack traces, or environment-derived secrets into the starter UI.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
