---
phase: 01-data-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/features/roster/domain/student-record.ts
  - src/features/roster/domain/import-issue.ts
  - src/features/roster/lib/normalize-vietnamese.ts
  - src/features/roster/lib/parse-birth-date.ts
  - src/features/roster/lib/map-headers.ts
  - src/features/roster/lib/sort-students.ts
  - tests/roster/normalize-vietnamese.test.ts
  - tests/roster/parse-birth-date.test.ts
  - tests/roster/map-headers.test.ts
autonomous: true
requirements:
  - IMPT-05
  - IMPT-06
user_setup: []
must_haves:
  truths:
    - "Canonical roster records keep both raw imported values and normalized values."
    - "Vietnamese sorting uses one deterministic comparator for `TEN`, `HO LOT`, and `MSHV`."
    - "Ambiguous birth-date strings are rejected instead of guessed."
  artifacts:
    - path: src/features/roster/domain/student-record.ts
      provides: "Canonical student data contract for import results"
      contains: "canonical"
    - path: src/features/roster/lib/normalize-vietnamese.ts
      provides: "Vietnamese-safe normalization helpers"
      exports:
        - normalizeVietnameseText
    - path: src/features/roster/lib/parse-birth-date.ts
      provides: "Deterministic date parser for Excel and text values"
      exports:
        - parseBirthDateValue
    - path: tests/roster/parse-birth-date.test.ts
      provides: "Coverage for supported and ambiguous date inputs"
      contains: "dd/MM/yyyy"
  key_links:
    - from: src/features/roster/lib/sort-students.ts
      to: src/features/roster/domain/student-record.ts
      via: "Comparator consumes canonical student fields"
      pattern: "CanonicalStudentRecord"
    - from: tests/roster/normalize-vietnamese.test.ts
      to: src/features/roster/lib/normalize-vietnamese.ts
      via: "Vitest import"
      pattern: "normalizeVietnameseText"
---

<objective>
Define the canonical roster data contracts and the pure transformation rules for Vietnamese text, headers, and birth dates.

Purpose: Phase 1 must settle normalization and parsing rules before workbook ingestion or UI preview can be trusted.
Output: Reusable domain types, deterministic helper functions, and focused unit tests for the highest-risk parsing rules.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define canonical roster and issue contracts</name>
  <files>src/features/roster/domain/student-record.ts, src/features/roster/domain/import-issue.ts</files>
  <read_first>.planning/phases/01-data-foundation/01-CONTEXT.md, src/features/roster/domain/student-record.ts, src/features/roster/domain/import-issue.ts</read_first>
  <action>Create `student-record.ts` with explicit raw and canonical structures for `className`, `studentCode`, `middleName`, `firstName`, `birthDate`, `birthPlace`, and optional `note`. The canonical record must preserve the raw row index and the original field values alongside normalized values. Create `import-issue.ts` with a discriminated model for `blocking`, `warning`, and `info` severities plus `row`, `column`, `code`, and `message` fields so server and UI layers share one issue contract.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - student-record.ts contains both `RawStudentRecord` and `CanonicalStudentRecord`
    - import-issue.ts contains the exact severity literals `blocking`, `warning`, and `info`
    - Canonical student fields include `birthDateIso` and `rowIndex`
  </acceptance_criteria>
  <done>The project has one canonical contract for parsed students and one shared contract for validation issues.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Vietnamese normalization, sorting, and birth-date parsing</name>
  <files>src/features/roster/lib/normalize-vietnamese.ts, src/features/roster/lib/parse-birth-date.ts, src/features/roster/lib/sort-students.ts, tests/roster/normalize-vietnamese.test.ts, tests/roster/parse-birth-date.test.ts</files>
  <read_first>.planning/phases/01-data-foundation/01-CONTEXT.md, src/features/roster/domain/student-record.ts, src/features/roster/lib/normalize-vietnamese.ts, src/features/roster/lib/parse-birth-date.ts, src/features/roster/lib/sort-students.ts, tests/roster/normalize-vietnamese.test.ts, tests/roster/parse-birth-date.test.ts</read_first>
  <action>Create `normalizeVietnameseText` that applies Unicode NFC normalization, trims outer whitespace, and collapses repeated interior whitespace without stripping Vietnamese diacritics. Create `toDisplayNameCase` so names are title-cased token-by-token while preserving hyphenated pieces. Create `sortStudentsByVietnameseName` using `new Intl.Collator('vi', { sensitivity: 'base', numeric: true })` and compare in the exact order `firstName`, `middleName`, `studentCode`. Create `parseBirthDateValue` that accepts Excel serial numbers, JavaScript `Date` objects, and text formats `dd/MM/yyyy`, `d/M/yyyy`, `dd-MM-yyyy`, `d-M-yyyy`, and `yyyy-MM-dd`; return ISO `yyyy-MM-dd` strings; reject ambiguous month/day strings with a `blocking` parse result instead of guessing. Add tests for Vietnamese whitespace cleanup, diacritic-safe casing, stable sort order, valid date formats, and ambiguous date rejection.</action>
  <verify>npm run test -- tests/roster/normalize-vietnamese.test.ts tests/roster/parse-birth-date.test.ts</verify>
  <acceptance_criteria>
    - normalize-vietnamese.ts contains `Intl.Collator('vi'`
    - parse-birth-date.ts contains the exact formats `dd/MM/yyyy`, `dd-MM-yyyy`, and `yyyy-MM-dd`
    - parse-birth-date.test.ts asserts an ambiguous text date is rejected
    - normalize-vietnamese.test.ts includes a case with Vietnamese diacritics
  </acceptance_criteria>
  <done>The project has deterministic text and date rules that later import code can call directly.</done>
</task>

<task type="auto">
  <name>Task 3: Implement header mapping with optional `GHI CHÚ` support</name>
  <files>src/features/roster/lib/map-headers.ts, tests/roster/map-headers.test.ts</files>
  <read_first>.planning/phases/01-data-foundation/01-CONTEXT.md, src/features/roster/domain/import-issue.ts, src/features/roster/lib/map-headers.ts, tests/roster/map-headers.test.ts</read_first>
  <action>Create `mapRosterHeaders` that reads the first worksheet row only, normalizes header cell text with the same Unicode/whitespace rules from Task 2, and maps only the exact required columns `Lớp`, `MSHV`, `HỌ LÓT`, `TÊN`, `NGÀY SINH`, and `NƠI SINH` plus optional `GHI CHÚ`. Support any column order, ignore unknown extra columns, and return blocking issues when required headers are missing. Do not add semantic aliases such as `Mã HV` or `Ngày sinh học viên`; this plan must keep header tolerance limited to casing, spaces, and Unicode forms. Add tests that cover column reordering, absent optional note column, extra columns, and missing required headers.</action>
  <verify>npm run test -- tests/roster/map-headers.test.ts</verify>
  <acceptance_criteria>
    - map-headers.ts contains the exact required header labels `Lớp`, `MSHV`, `HỌ LÓT`, `TÊN`, `NGÀY SINH`, and `NƠI SINH`
    - map-headers.ts contains `GHI CHÚ` as an optional column
    - map-headers.test.ts contains a test where the optional note column is absent
    - map-headers.test.ts contains a test where a required header produces a blocking issue
  </acceptance_criteria>
  <done>The workbook reader can resolve the supported schema deterministically before row parsing begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npm run test -- tests/roster/normalize-vietnamese.test.ts tests/roster/parse-birth-date.test.ts passes
- [ ] npm run test -- tests/roster/map-headers.test.ts passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Canonical student helpers are pure and test-covered
- The codebase has one shared definition of supported headers, normalization rules, and date parsing behavior
</success_criteria>

<threat_model>
<trust_boundaries>
- Workbook cell values -> canonical domain helpers
- Raw student text -> deterministic Vietnamese normalization and sorting
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>01-02-T1</threat_id>
    <category>Tampering</category>
    <component>Vietnamese normalization helpers</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Keep raw field values in the canonical contract and confine normalization to explicit helper functions so later plans can audit every canonical change instead of mutating input in place.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>01-02-T2</threat_id>
    <category>Integrity</category>
    <component>Birth-date parser</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Accept only the explicitly listed date formats and reject ambiguous strings with a blocking result. Back this with unit tests so later import code cannot silently reinterpret dates.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/01-data-foundation/01-02-SUMMARY.md`
</output>
