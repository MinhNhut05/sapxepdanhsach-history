---
phase: 01-data-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/features/roster/server/file-guard.ts
  - src/features/roster/server/read-workbook.ts
  - src/features/roster/server/row-validation.ts
  - src/features/roster/server/import-roster.ts
  - src/app/api/rosters/import/route.ts
  - tests/roster/import-roster.test.ts
  - tests/api/rosters-import-route.test.ts
autonomous: true
requirements:
  - IMPT-02
  - IMPT-03
  - IMPT-04
  - SAFE-01
  - SAFE-02
user_setup: []
must_haves:
  truths:
    - "The server accepts only supported `.xlsx` uploads within a concrete size limit."
    - "Workbook import stops on blocking row or schema errors and returns row-level details."
    - "Imports with only warnings or info still return parsed students and issue summaries."
  artifacts:
    - path: src/app/api/rosters/import/route.ts
      provides: "Server-owned import endpoint"
      exports:
        - POST
    - path: src/features/roster/server/import-roster.ts
      provides: "Central orchestration for workbook parsing and validation"
      min_lines: 40
    - path: src/features/roster/server/file-guard.ts
      provides: "Upload type and size validation"
      contains: "MAX_UPLOAD_BYTES"
    - path: tests/api/rosters-import-route.test.ts
      provides: "Endpoint behavior coverage for success and rejection paths"
      contains: "415"
  key_links:
    - from: src/app/api/rosters/import/route.ts
      to: src/features/roster/server/import-roster.ts
      via: "Route delegates all workbook parsing to the import service"
      pattern: "importRosterWorkbook"
    - from: src/features/roster/server/import-roster.ts
      to: src/features/roster/lib/map-headers.ts
      via: "Import service uses the shared header mapper"
      pattern: "mapRosterHeaders"
---

<objective>
Build the server-side roster import pipeline that reads `.xlsx` files, validates rows, and returns a trusted canonical result.

Purpose: Phase 1 must enforce file and schema validation on the server so later allocation logic cannot bypass import rules.
Output: File guard helpers, workbook reader, row-validation/import service, API route, and integration tests for upload rejection and parsing behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce upload file guards and workbook-loading rules</name>
  <files>src/features/roster/server/file-guard.ts, src/features/roster/server/read-workbook.ts</files>
  <read_first>.planning/phases/01-data-foundation/01-CONTEXT.md, src/features/roster/lib/map-headers.ts, src/features/roster/server/file-guard.ts, src/features/roster/server/read-workbook.ts</read_first>
  <action>Create `file-guard.ts` with `MAX_UPLOAD_BYTES = 10 * 1024 * 1024`, allow only filenames ending in `.xlsx`, and accept the MIME types `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` and `application/octet-stream` only when the extension is `.xlsx`. Return explicit rejection codes for missing file, wrong type, and oversized payloads. Create `read-workbook.ts` using `ExcelJS` to read the uploaded buffer, select only the first worksheet, require the header row to be worksheet row 1, and expose a normalized row iterator for downstream validation.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - file-guard.ts contains `MAX_UPLOAD_BYTES = 10 * 1024 * 1024`
    - file-guard.ts contains the MIME type `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
    - read-workbook.ts contains logic that selects only the first worksheet
    - read-workbook.ts rejects sheets whose header row is not row 1
  </acceptance_criteria>
  <done>The server has one concrete gate for supported roster uploads before any row parsing begins.</done>
</task>

<task type="auto">
  <name>Task 2: Implement canonical row validation and import orchestration</name>
  <files>src/features/roster/server/row-validation.ts, src/features/roster/server/import-roster.ts, tests/roster/import-roster.test.ts</files>
  <read_first>.planning/phases/01-data-foundation/01-CONTEXT.md, src/features/roster/domain/student-record.ts, src/features/roster/domain/import-issue.ts, src/features/roster/lib/normalize-vietnamese.ts, src/features/roster/lib/parse-birth-date.ts, src/features/roster/lib/map-headers.ts, src/features/roster/server/row-validation.ts, src/features/roster/server/import-roster.ts, tests/roster/import-roster.test.ts</read_first>
  <action>Create `row-validation.ts` and `import-roster.ts` so the import service produces `{ ok, summary, students, issues }`. Validate every required field, block duplicate `MSHV`, block ambiguous or invalid birth dates, block malformed rows, and block missing required headers. Emit warnings for blank rows inside the data region and for same full name plus same birth date with different `MSHV`. Emit info issues when canonical text differs from raw text after normalization. Preserve raw values in the returned student records and sort valid rows with the shared Vietnamese comparator before returning them. Add tests that cover valid imports with and without `GHI CHÚ`, duplicate `MSHV`, same-name warning, blank-row warning, and normalization info messages.</action>
  <verify>npm run test -- tests/roster/import-roster.test.ts</verify>
  <acceptance_criteria>
    - import-roster.ts returns the keys `ok`, `summary`, `students`, and `issues`
    - row-validation.ts contains duplicate `MSHV` blocking logic
    - import-roster.test.ts covers both presence and absence of `GHI CHÚ`
    - import-roster.test.ts contains a same-name-plus-birth-date warning case
  </acceptance_criteria>
  <done>The server can turn a workbook into trusted canonical data plus actionable validation feedback.</done>
</task>

<task type="auto">
  <name>Task 3: Expose the import pipeline through a guarded API route</name>
  <files>src/app/api/rosters/import/route.ts, tests/api/rosters-import-route.test.ts</files>
  <read_first>src/features/roster/server/file-guard.ts, src/features/roster/server/import-roster.ts, src/app/api/rosters/import/route.ts, tests/api/rosters-import-route.test.ts</read_first>
  <action>Create `src/app/api/rosters/import/route.ts` with a `POST` handler that reads multipart form data field `file`, applies the file guard before parsing, calls `importRosterWorkbook`, and returns JSON with these exact statuses: `200` when parsing succeeds with only warning/info issues, `422` when blocking validation issues exist, `415` for unsupported file types, `413` for oversized uploads, and `400` when the file is missing. The route must not expose any alternate code path that trusts client-side validation. Add route tests that assert each status code and confirm blocking issues are surfaced as row-level JSON entries.</action>
  <verify>npm run test -- tests/api/rosters-import-route.test.ts</verify>
  <acceptance_criteria>
    - route.ts exports `POST`
    - route.ts returns `422` for blocking validation issues
    - route.ts returns `415` for unsupported file types and `413` for oversized uploads
    - rosters-import-route.test.ts contains assertions for `200`, `400`, `413`, `415`, and `422`
  </acceptance_criteria>
  <done>The Phase 1 server boundary is real: only validated roster data can enter the app flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npm run test -- tests/roster/import-roster.test.ts passes
- [ ] npm run test -- tests/api/rosters-import-route.test.ts passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The import route rejects unsupported, oversized, and structurally invalid uploads
- The import route returns row-level issue metadata that later UI code can render directly
</success_criteria>

<threat_model>
<trust_boundaries>
- Browser-uploaded file -> Next.js route handler
- Route handler -> workbook parser and validation pipeline
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>01-03-T1</threat_id>
    <category>Denial of Service</category>
    <component>Upload endpoint</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Reject uploads larger than `10 * 1024 * 1024` bytes before workbook parsing and return HTTP 413 so oversized files cannot consume parser time.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>01-03-T2</threat_id>
    <category>Tampering</category>
    <component>File type validation</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Require the `.xlsx` extension plus an allowed MIME combination and return HTTP 415 for unsupported payloads so renamed or unexpected file formats do not enter the parser.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>01-03-T3</threat_id>
    <category>Elevation of Privilege</category>
    <component>Import route / downstream workflow gate</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Route all import requests through `importRosterWorkbook` and return HTTP 422 for blocking issues; do not provide any alternate API branch that trusts client-generated validation summaries.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/01-data-foundation/01-03-SUMMARY.md`
</output>
