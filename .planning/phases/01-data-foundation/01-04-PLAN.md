---
phase: 01-data-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/app/page.tsx
  - src/features/roster/ui/upload-panel.tsx
  - src/features/roster/ui/import-summary.tsx
  - src/features/roster/ui/import-preview-table.tsx
  - src/features/roster/ui/import-issues-table.tsx
  - src/features/roster/ui/import-state.ts
  - tests/ui/upload-panel.test.tsx
autonomous: true
requirements:
  - IMPT-01
  - IMPT-04
user_setup: []
must_haves:
  truths:
    - "A user can upload a valid `.xlsx` roster from the homepage and see parsed student data."
    - "A user sees blocking, warning, and info issues in distinct UI sections with row-level detail."
    - "The page displays only server-validated import results rather than client-invented preview data."
  artifacts:
    - path: src/features/roster/ui/upload-panel.tsx
      provides: "Client upload workflow for the homepage"
      min_lines: 40
    - path: src/features/roster/ui/import-preview-table.tsx
      provides: "Parsed student preview table"
      contains: "MSHV"
    - path: src/features/roster/ui/import-issues-table.tsx
      provides: "Validation issue rendering grouped by severity"
      contains: "blocking"
    - path: tests/ui/upload-panel.test.tsx
      provides: "UI coverage for success and validation-error states"
      contains: "fetch"
  key_links:
    - from: src/features/roster/ui/upload-panel.tsx
      to: /api/rosters/import
      via: "client fetch POST"
      pattern: "/api/rosters/import"
    - from: src/app/page.tsx
      to: src/features/roster/ui/upload-panel.tsx
      via: "homepage composition"
      pattern: "UploadPanel"
---

<objective>
Deliver the operator-facing upload page that calls the import API and renders parsed data plus validation feedback.

Purpose: Phase 1 is not complete until a user can actually upload a roster and inspect the trusted result before allocation exists.
Output: Upload client component, import summary, preview table, issue table, page integration, and UI tests for success and error states.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the homepage upload workflow</name>
  <files>src/app/page.tsx, src/features/roster/ui/upload-panel.tsx, src/features/roster/ui/import-state.ts</files>
  <read_first>src/app/page.tsx, src/app/globals.css, src/app/api/rosters/import/route.ts, src/features/roster/ui/upload-panel.tsx, src/features/roster/ui/import-state.ts</read_first>
  <action>Create `import-state.ts` with the exact client states `idle`, `uploading`, `success`, and `error`. Build `upload-panel.tsx` as a client component with a file input restricted to `.xlsx`, a submit button, loading/disabled states, and a `fetch('/api/rosters/import', { method: 'POST' })` call that sends the chosen file as `FormData`. Update `src/app/page.tsx` to compose this upload panel into the server-rendered shell from Plan 01. The UI must always render server response data; do not precompute validation outcomes on the client.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - import-state.ts contains the exact state literals `idle`, `uploading`, `success`, and `error`
    - upload-panel.tsx contains `accept=\".xlsx\"`
    - upload-panel.tsx contains `fetch('/api/rosters/import'`
    - src/app/page.tsx renders `UploadPanel`
  </acceptance_criteria>
  <done>The homepage can submit roster files to the Phase 1 import API and track the response lifecycle.</done>
</task>

<task type="auto">
  <name>Task 2: Render parsed student previews and validation feedback</name>
  <files>src/features/roster/ui/import-summary.tsx, src/features/roster/ui/import-preview-table.tsx, src/features/roster/ui/import-issues-table.tsx</files>
  <read_first>src/features/roster/domain/student-record.ts, src/features/roster/domain/import-issue.ts, src/features/roster/ui/import-summary.tsx, src/features/roster/ui/import-preview-table.tsx, src/features/roster/ui/import-issues-table.tsx</read_first>
  <action>Create `import-summary.tsx` to show total rows read, valid students returned, and counts for blocking/warning/info issues. Create `import-preview-table.tsx` with columns for `Lớp`, `MSHV`, full name, `Ngày sinh`, `Nơi sinh`, and optional `GHI CHÚ`, using canonical values returned by the server. Create `import-issues-table.tsx` that groups issues by severity, shows row and column when present, and displays blocking issues separately from warnings and info so operators can fix the source file in one pass. Ensure the page shows the preview table only when the API response is a valid import result.</action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - import-summary.tsx contains counts for blocking, warning, and info issues
    - import-preview-table.tsx contains the exact headings `Lớp`, `MSHV`, `Ngày sinh`, and `Nơi sinh`
    - import-issues-table.tsx renders severity groups for `blocking`, `warning`, and `info`
    - import-preview-table.tsx renders `GHI CHÚ` conditionally instead of requiring it
  </acceptance_criteria>
  <done>The homepage exposes both the trusted parsed roster and the full validation context required by Phase 1.</done>
</task>

<task type="auto">
  <name>Task 3: Add UI tests for successful and failing imports</name>
  <files>tests/ui/upload-panel.test.tsx</files>
  <read_first>src/features/roster/ui/upload-panel.tsx, src/features/roster/ui/import-summary.tsx, src/features/roster/ui/import-preview-table.tsx, src/features/roster/ui/import-issues-table.tsx, tests/ui/upload-panel.test.tsx</read_first>
  <action>Create `tests/ui/upload-panel.test.tsx` with mocked `fetch` responses for two flows: a successful import with parsed students and a blocking validation response with row-level issues. Assert that the component disables the submit button while uploading, renders the preview table on success, and renders the blocking issue table when the API returns a validation failure payload. Keep the tests at the component level; do not add Playwright in this phase.</action>
  <verify>npm run test -- tests/ui/upload-panel.test.tsx</verify>
  <acceptance_criteria>
    - upload-panel.test.tsx mocks `fetch`
    - upload-panel.test.tsx asserts the submit button is disabled during upload
    - upload-panel.test.tsx asserts parsed student data is rendered on success
    - upload-panel.test.tsx asserts blocking validation messages are rendered on failure
  </acceptance_criteria>
  <done>The user-facing upload flow has executable regression coverage for both happy and unhappy paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint passes
- [ ] npm run typecheck passes
- [ ] npm run test -- tests/ui/upload-panel.test.tsx passes
- [ ] npm run build passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The homepage lets users upload a roster and inspect canonical parsed data
- Validation errors are visible in one operator-friendly place before any downstream workflow exists
</success_criteria>

<threat_model>
<trust_boundaries>
- Browser UI -> import API response payload
- Client-rendered preview -> server-validated canonical roster data
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>01-04-T1</threat_id>
    <category>Integrity</category>
    <component>Upload panel state management</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Drive UI state exclusively from the import API response and expose explicit `idle/uploading/success/error` states so the preview cannot show unvalidated client-side guesses.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>01-04-T2</threat_id>
    <category>Information Disclosure</category>
    <component>Validation issue rendering</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Render row, column, and safe field values only. Do not surface raw stack traces or internal parser exceptions in the issue table.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/01-data-foundation/01-04-SUMMARY.md`
</output>
