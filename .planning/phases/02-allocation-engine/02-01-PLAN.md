---
phase: 02-allocation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - prisma/schema.prisma
  - src/lib/prisma.ts
  - src/features/allocation/domain/allocation-types.ts
autonomous: true
requirements:
  - HIST-01
  - HIST-03
  - HIST-04
user_setup: []
must_haves:
  truths:
    - "The repository has a Prisma-backed persistence foundation for saved allocation runs."
    - "Allocation runs can store reproducible input and output snapshots plus metadata in one authoritative record."
    - "Phase 2 code has shared allocation contracts that the engine, server route, and UI can reuse."
  artifacts:
    - path: prisma/schema.prisma
      provides: "AllocationRun persistence model with metadata and JSON snapshot fields"
      contains: "model AllocationRun"
    - path: src/lib/prisma.ts
      provides: "Singleton Prisma client for server-side allocation services"
      contains: "new PrismaClient"
    - path: src/features/allocation/domain/allocation-types.ts
      provides: "Shared strategy keys and run payload contracts"
      contains: "AllocationStrategyKey"
  key_links:
    - from: src/lib/prisma.ts
      to: prisma/schema.prisma
      via: "Generated Prisma client serves the AllocationRun model"
      pattern: "PrismaClient"
    - from: src/features/allocation/domain/allocation-types.ts
      to: prisma/schema.prisma
      via: "Application contracts mirror persisted strategy and snapshot metadata"
      pattern: "AllocationRunSummary"
---

<objective>
Establish the persistence and contract foundation for deterministic allocation runs.

Purpose: Phase 2 cannot produce reproducible saved results until the repo has Prisma, a concrete AllocationRun schema, and one shared set of run contracts.
Output: Prisma dependencies, AllocationRun schema, DB client singleton, and shared allocation type definitions that later plans will build on.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-allocation-engine/02-RESEARCH.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Prisma tooling and wire a cached server client</name>
  <files>package.json, package-lock.json, src/lib/prisma.ts</files>
  <read_first>CLAUDE.md, package.json, package-lock.json, src/features/roster/server/import-roster.ts, src/app/api/rosters/import/route.ts</read_first>
  <action>Add `@prisma/client` as a runtime dependency and `prisma` as a dev dependency in `package.json` and `package-lock.json`. Create `src/lib/prisma.ts` that exports a cached `prisma` singleton built from `new PrismaClient()` and uses `globalThis` caching outside production so Next.js dev reloads do not create extra connections. Keep the file server-only and do not introduce any allocation logic into it.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - package.json contains `\"@prisma/client\"`
    - package.json contains `\"prisma\"`
    - src/lib/prisma.ts contains `new PrismaClient(`
    - src/lib/prisma.ts contains `globalThis`
  </acceptance_criteria>
  <done>The repo can generate and reuse a Prisma client safely from server code.</done>
</task>

<task type="auto">
  <name>Task 2: Define the AllocationRun schema for reproducible saved runs</name>
  <files>prisma/schema.prisma</files>
  <read_first>CLAUDE.md, .planning/phases/02-allocation-engine/02-RESEARCH.md, prisma/schema.prisma</read_first>
  <action>Create `prisma/schema.prisma` with a PostgreSQL datasource using `env(\"DATABASE_URL\")`, a `prisma-client-js` generator, and an `AllocationRun` model. The model must include `id` with `cuid()`, `createdAt` with `now()`, `sourceFileName`, optional `sourceSheetName`, `roomCount`, `strategy`, `totalStudents`, `algorithmVersion`, `rosterFingerprint`, `inputSnapshot` as `Json`, `resultSnapshot` as `Json`, and `summary` as `Json`. `sourceSheetName` must preserve the original worksheet label from imported workbooks such as the K19A baseline sheet `Worksheet 1`. Add indexes on `createdAt` and `strategy`. Do not introduce normalized assignment tables in this plan; Phase 2 should persist one authoritative run record.</action>
  <verify>npx prisma validate</verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `provider = \"postgresql\"`
    - prisma/schema.prisma contains `model AllocationRun`
    - prisma/schema.prisma contains `inputSnapshot Json`
    - prisma/schema.prisma contains `resultSnapshot Json`
    - prisma/schema.prisma contains `rosterFingerprint`
    - prisma/schema.prisma contains `sourceSheetName`
  </acceptance_criteria>
  <done>The database schema can store one saved allocation run with enough metadata to reproduce it later.</done>
</task>

<task type="auto">
  <name>Task 3: Add shared allocation contracts and apply the schema</name>
  <files>src/features/allocation/domain/allocation-types.ts, prisma/schema.prisma</files>
  <read_first>.planning/phases/02-allocation-engine/02-RESEARCH.md, src/features/roster/domain/student-record.ts, src/features/allocation/domain/allocation-types.ts, prisma/schema.prisma</read_first>
  <action>Create `src/features/allocation/domain/allocation-types.ts` with the exact strategy key union `even_mix | class_grouped | representative_ratio`, a room-capacity contract, an allocated-student contract that carries canonical student data plus `roomNumber`, `seatIndex`, and `candidateNumber`, and an `AllocationRunSummary` contract with `totalStudents`, `roomCount`, `maxRoomSize`, `minRoomSize`, and `sizeSpread`. After finishing the schema edits, run the mandatory blocking database step `npx prisma db push`. If Prisma reports destructive prompts that cannot run non-interactively in the local dev database, rerun with `npx prisma db push --accept-data-loss` and capture that choice in the summary.</action>
  <verify>npx prisma db push && npx prisma validate && npm run typecheck</verify>
  <acceptance_criteria>
    - allocation-types.ts contains `even_mix`
    - allocation-types.ts contains `class_grouped`
    - allocation-types.ts contains `representative_ratio`
    - allocation-types.ts contains `AllocationRunSummary`
    - prisma/schema.prisma contains `summary Json`
  </acceptance_criteria>
  <done>The allocation domain and persistence layer now share one concrete vocabulary, and the live database schema matches the Prisma schema.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npx prisma validate passes
- [ ] npx prisma db push passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The repo has a usable Prisma foundation for saved allocation runs
- Later plans can import shared allocation contracts without redefining strategy or summary shapes
</success_criteria>

<threat_model>
<trust_boundaries>
- Next.js server code -> PostgreSQL database via Prisma
- Saved allocation snapshots -> persisted JSON fields inside AllocationRun
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>02-01-T1</threat_id>
    <category>Tampering</category>
    <component>Prisma schema and generated client</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Define the exact AllocationRun fields in `prisma/schema.prisma`, validate the schema with `npx prisma validate`, and run the mandatory schema push before verification so later code cannot drift from an unapplied local schema.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>02-01-T2</threat_id>
    <category>Information Disclosure</category>
    <component>Saved input and result snapshots</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Persist only the canonical allocation payload and explicit metadata fields needed for reproducibility. Do not store arbitrary request envelopes, stack traces, or unrelated server state in the JSON snapshot columns.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/02-allocation-engine/02-01-SUMMARY.md`
</output>
