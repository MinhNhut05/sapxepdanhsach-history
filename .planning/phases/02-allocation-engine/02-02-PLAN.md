---
phase: 02-allocation-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/features/allocation/domain/build-room-capacities.ts
  - src/features/allocation/domain/assign-even-mix.ts
  - src/features/allocation/domain/assign-class-grouped.ts
  - src/features/allocation/domain/assign-representative-ratio.ts
  - src/features/allocation/domain/generate-candidate-numbers.ts
  - src/features/allocation/domain/validate-allocation-result.ts
  - src/features/allocation/domain/create-allocation-run.ts
  - tests/allocation/build-room-capacities.test.ts
  - tests/allocation/strategy-algorithms.test.ts
  - tests/allocation/create-allocation-run.test.ts
autonomous: true
requirements:
  - ALOC-02
  - ALOC-03
  - ALOC-04
  - ALOC-05
  - ALOC-06
  - ALOC-07
  - ALOC-08
user_setup: []
must_haves:
  truths:
    - "The same canonical roster and settings always produce the same room assignments and candidate numbers."
    - "Every strategy keeps room sizes within one student of each other while honoring its own allocation objective."
    - "Candidate numbers are derived only after per-room Vietnamese ordering is finalized."
  artifacts:
    - path: src/features/allocation/domain/create-allocation-run.ts
      provides: "Pure orchestration for deterministic allocation runs"
      contains: "createAllocationRun"
    - path: src/features/allocation/domain/generate-candidate-numbers.ts
      provides: "Pxx-yyy candidate number generation"
      contains: "candidateNumber"
    - path: tests/allocation/strategy-algorithms.test.ts
      provides: "Deterministic strategy coverage for all three allocation modes"
      contains: "representative_ratio"
  key_links:
    - from: src/features/allocation/domain/create-allocation-run.ts
      to: src/features/roster/lib/sort-students.ts
      via: "Allocator reuses the canonical Vietnamese comparator from Phase 1"
      pattern: "sortStudentsByVietnameseName"
    - from: src/features/allocation/domain/create-allocation-run.ts
      to: src/features/allocation/domain/build-room-capacities.ts
      via: "Run orchestration derives target room sizes before assigning students"
      pattern: "buildRoomCapacities"
---

<objective>
Implement the pure allocation engine, deterministic strategy rules, and unit coverage for reproducible exam-room runs.

Purpose: Phase 2 only becomes trustworthy once the core engine is pure, deterministic, strategy-aware, and validated independently from UI or persistence.
Output: Room-capacity builder, three deterministic assignment strategies, SBD generation, invariant validation, and unit tests for reproducibility and fairness bounds.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-allocation-engine/02-RESEARCH.md
@.planning/phases/02-allocation-engine/02-01-SUMMARY.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build deterministic room capacities and allocation orchestration</name>
  <files>src/features/allocation/domain/build-room-capacities.ts, src/features/allocation/domain/create-allocation-run.ts</files>
  <read_first>.planning/phases/02-allocation-engine/02-RESEARCH.md, src/features/allocation/domain/allocation-types.ts, src/features/roster/domain/student-record.ts, src/features/roster/lib/sort-students.ts, src/features/allocation/domain/build-room-capacities.ts, src/features/allocation/domain/create-allocation-run.ts</read_first>
  <action>Create `build-room-capacities.ts` that returns room capacity records using the exact rule `base = Math.floor(totalStudents / roomCount)` and `remainder = totalStudents % roomCount`, with rooms `1..remainder` receiving `base + 1` and the rest receiving `base`. Create `create-allocation-run.ts` that sorts students once with `sortStudentsByVietnameseName`, selects the strategy implementation by the exact keys `even_mix`, `class_grouped`, and `representative_ratio`, and returns a pure allocation result without reading environment state or mutating the input array. Include a regression assertion path for the real K19A baseline workbook (`Worksheet 1`, `272` students, classes `A1..A7`) where `roomCount=13` yields capacities `12x21` and `1x20`.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - build-room-capacities.ts contains `Math.floor(totalStudents / roomCount)`
    - build-room-capacities.ts contains `totalStudents % roomCount`
    - create-allocation-run.ts contains `sortStudentsByVietnameseName`
    - create-allocation-run.ts contains `switch (strategy)` or an equivalent exact strategy dispatch
  </acceptance_criteria>
  <done>The allocation engine has one deterministic entry point and one explicit room-balance rule.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the three allocation strategies with exact deterministic rules</name>
  <files>src/features/allocation/domain/assign-even-mix.ts, src/features/allocation/domain/assign-class-grouped.ts, src/features/allocation/domain/assign-representative-ratio.ts</files>
  <read_first>.planning/phases/02-allocation-engine/02-RESEARCH.md, src/features/allocation/domain/allocation-types.ts, src/features/allocation/domain/build-room-capacities.ts, src/features/allocation/domain/assign-even-mix.ts, src/features/allocation/domain/assign-class-grouped.ts, src/features/allocation/domain/assign-representative-ratio.ts</read_first>
  <action>Create `assign-even-mix.ts` that assigns the sorted student list round-robin across room numbers in ascending order. Create `assign-class-grouped.ts` that groups students by canonical class name, sorts classes ascending, sorts students within each class canonically, and fills room capacities while keeping each class split across as few rooms as possible. Create `assign-representative-ratio.ts` that computes per-class proportional quotas for each room using a largest-remainder approach, breaks quota ties by lower room number and canonical class name, and fills room slots with canonical student order. Keep all tie-breaks explicit and deterministic.</action>
  <verify>npm run test -- tests/allocation/strategy-algorithms.test.ts</verify>
  <acceptance_criteria>
    - assign-even-mix.ts contains the exact strategy key `even_mix`
    - assign-class-grouped.ts contains grouping by canonical class name
    - assign-representative-ratio.ts contains a largest-remainder quota step
    - strategy-algorithms.test.ts covers `even_mix`, `class_grouped`, and `representative_ratio`
  </acceptance_criteria>
  <done>Each supported allocation mode is implemented with a measurable, reproducible rule set instead of vague heuristics.</done>
</task>

<task type="auto">
  <name>Task 3: Generate candidate numbers, validate invariants, and prove reproducibility with tests</name>
  <files>src/features/allocation/domain/generate-candidate-numbers.ts, src/features/allocation/domain/validate-allocation-result.ts, tests/allocation/build-room-capacities.test.ts, tests/allocation/create-allocation-run.test.ts, tests/allocation/strategy-algorithms.test.ts</files>
  <read_first>.planning/phases/02-allocation-engine/02-RESEARCH.md, src/features/allocation/domain/create-allocation-run.ts, src/features/allocation/domain/generate-candidate-numbers.ts, src/features/allocation/domain/validate-allocation-result.ts, tests/allocation/build-room-capacities.test.ts, tests/allocation/create-allocation-run.test.ts, tests/allocation/strategy-algorithms.test.ts</read_first>
  <action>Create `generate-candidate-numbers.ts` so each room is re-sorted with the canonical Vietnamese comparator before assigning `candidateNumber` values using `P${roomNumber.padStart(2, '0')}-${seatIndex.padStart(3, '0')}`. Create `validate-allocation-result.ts` that blocks missing assignments, duplicate candidate numbers, room-count mismatches, capacity overruns, and size spreads greater than `1`. Add tests that verify repeated runs with the same fixture and same settings return identical room allocations and candidate numbers, that `maxRoomSize - minRoomSize <= 1`, and that the generated values match the `Pxx-yyy` pattern.</action>
  <verify>npm run test -- tests/allocation/build-room-capacities.test.ts tests/allocation/strategy-algorithms.test.ts tests/allocation/create-allocation-run.test.ts</verify>
  <acceptance_criteria>
    - generate-candidate-numbers.ts contains `padStart(2, '0')`
    - generate-candidate-numbers.ts contains `padStart(3, '0')`
    - validate-allocation-result.ts contains duplicate candidate number detection
    - create-allocation-run.test.ts asserts repeated runs return identical results
    - create-allocation-run.test.ts asserts the `Pxx-yyy` format
    - build-room-capacities.test.ts asserts K19A baseline distribution (`272/13` => `12x21`, `1x20`)
  </acceptance_criteria>
  <done>The allocation engine is reproducible, rule-checked, and protected by domain-level regression tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npm run test -- tests/allocation/build-room-capacities.test.ts tests/allocation/strategy-algorithms.test.ts tests/allocation/create-allocation-run.test.ts passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The repo has one deterministic allocation engine for all three strategies
- Repeated runs with the same input and settings produce the same room assignments and candidate numbers
</success_criteria>

<threat_model>
<trust_boundaries>
- Canonical roster input -> pure allocation engine
- Strategy-specific assignment output -> candidate number generation and invariant validation
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>02-02-T1</threat_id>
    <category>Integrity</category>
    <component>Allocation strategy implementations</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Keep the engine pure, sort students canonically before assignment, and codify every tie-break rule in code and tests so repeated runs cannot drift or depend on runtime ordering quirks.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>02-02-T2</threat_id>
    <category>Tampering</category>
    <component>Candidate number generation and result validation</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Generate `Pxx-yyy` values only after final per-room ordering and run a dedicated validation pass that rejects duplicate candidate numbers, missing students, capacity overruns, and invalid room spreads before any result can be persisted.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/02-allocation-engine/02-02-SUMMARY.md`
</output>
