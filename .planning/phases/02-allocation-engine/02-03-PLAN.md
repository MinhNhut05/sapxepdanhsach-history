---
phase: 02-allocation-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/features/allocation/server/allocation-request.ts
  - src/features/allocation/server/run-allocation.ts
  - src/features/allocation/server/save-allocation-run.ts
  - src/app/api/allocations/route.ts
  - tests/api/allocations-route.test.ts
autonomous: true
requirements:
  - ALOC-01
  - ALOC-05
  - HIST-01
  - HIST-03
  - HIST-04
user_setup: []
must_haves:
  truths:
    - "A server-owned allocation service validates the request, runs the pure engine, and persists the saved result."
    - "Saved runs include enough metadata and snapshots to explain how the result was produced."
    - "Repeated API requests with the same canonical input and settings return the same assignment content."
  artifacts:
    - path: src/app/api/allocations/route.ts
      provides: "HTTP entry point for creating saved allocation runs"
      exports:
        - POST
    - path: src/features/allocation/server/save-allocation-run.ts
      provides: "Prisma-backed persistence for AllocationRun records"
      contains: "prisma.allocationRun.create"
    - path: tests/api/allocations-route.test.ts
      provides: "Integration coverage for request validation, persistence wiring, and deterministic responses"
      contains: "/api/allocations"
  key_links:
    - from: src/features/allocation/server/run-allocation.ts
      to: src/features/allocation/domain/create-allocation-run.ts
      via: "Server service delegates room assignment to the pure domain engine"
      pattern: "createAllocationRun"
    - from: src/app/api/allocations/route.ts
      to: src/features/allocation/server/save-allocation-run.ts
      via: "Route persists saved runs after successful allocation"
      pattern: "saveAllocationRun"
---

<objective>
Expose deterministic allocation as a persisted server workflow and API route.

Purpose: Phase 2 is not complete until a user can submit a room count and strategy, trigger the server-owned allocator, and receive a saved run with reproducible metadata.
Output: Request validation, server orchestration, Prisma persistence, and a tested `/api/allocations` route.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-allocation-engine/02-RESEARCH.md
@.planning/phases/02-allocation-engine/02-01-SUMMARY.md
@.planning/phases/02-allocation-engine/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate allocation requests and build the server orchestration service</name>
  <files>src/features/allocation/server/allocation-request.ts, src/features/allocation/server/run-allocation.ts</files>
  <read_first>.planning/phases/02-allocation-engine/02-RESEARCH.md, src/features/roster/domain/student-record.ts, src/features/allocation/domain/allocation-types.ts, src/features/allocation/domain/create-allocation-run.ts, src/features/allocation/server/allocation-request.ts, src/features/allocation/server/run-allocation.ts</read_first>
  <action>Create `allocation-request.ts` with a Zod schema that accepts `sourceFileName`, optional `sourceSheetName`, `roomCount`, `strategy`, and a non-empty canonical `students` array. `roomCount` must be an integer between `1` and `99`, and `strategy` must accept only `even_mix`, `class_grouped`, or `representative_ratio`. Create `run-allocation.ts` that parses the request, rejects impossible but well-formed configs such as `roomCount > students.length` or any room capacity that would require a `seatIndex` above `999`, computes a stable SHA-256 `rosterFingerprint` from the canonical student payload, sets an explicit `algorithmVersion` string, and delegates the actual assignment work to `createAllocationRun`.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - allocation-request.ts contains `z.object(`
    - allocation-request.ts contains `sourceSheetName`
    - allocation-request.ts contains `even_mix`
    - allocation-request.ts contains `class_grouped`
    - allocation-request.ts contains `representative_ratio`
    - run-allocation.ts contains `createHash(\"sha256\")` or `createHash('sha256')`
    - run-allocation.ts contains `algorithmVersion`
  </acceptance_criteria>
  <done>The server can accept only supported allocation requests and compute stable run metadata before persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Persist AllocationRun records and return a UI-ready saved payload</name>
  <files>src/features/allocation/server/save-allocation-run.ts</files>
  <read_first>src/lib/prisma.ts, prisma/schema.prisma, src/features/allocation/domain/allocation-types.ts, src/features/allocation/server/run-allocation.ts, src/features/allocation/server/save-allocation-run.ts</read_first>
  <action>Create `save-allocation-run.ts` that calls `prisma.allocationRun.create` with `sourceFileName`, `sourceSheetName`, `roomCount`, `strategy`, `totalStudents`, `algorithmVersion`, `rosterFingerprint`, `inputSnapshot`, `resultSnapshot`, and `summary`. Return a payload shaped for the UI with `id`, `createdAt`, `strategy`, `roomCount`, `summary`, and the saved room assignments from `resultSnapshot`. Do not make the route or UI reconstruct rooms from raw DB primitives; persistence should return the authoritative saved run shape directly.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - save-allocation-run.ts contains `prisma.allocationRun.create`
    - save-allocation-run.ts writes `sourceSheetName`
    - save-allocation-run.ts writes `inputSnapshot`
    - save-allocation-run.ts writes `resultSnapshot`
    - save-allocation-run.ts returns `createdAt`
  </acceptance_criteria>
  <done>The persistence layer writes one authoritative AllocationRun record and hands back a payload the UI can render without extra recomputation.</done>
</task>

<task type="auto">
  <name>Task 3: Add the `/api/allocations` route and verify deterministic responses</name>
  <files>src/app/api/allocations/route.ts, tests/api/allocations-route.test.ts</files>
  <read_first>src/features/allocation/server/allocation-request.ts, src/features/allocation/server/run-allocation.ts, src/features/allocation/server/save-allocation-run.ts, src/app/api/allocations/route.ts, tests/api/allocations-route.test.ts</read_first>
  <action>Create `src/app/api/allocations/route.ts` with a `POST` handler that reads JSON, validates it with the request schema, runs the allocation service, persists the run, and returns JSON. Use status `200` for success, `400` for malformed payloads, and `422` for impossible but well-formed allocation configs such as `roomCount > students.length`. Add `tests/api/allocations-route.test.ts` that covers a successful saved run, malformed payload rejection, impossible room-count rejection, and two repeated successful requests proving that the returned room assignments and candidate numbers are identical even though the saved run IDs differ. Include a K19A baseline regression assertion where `272` students and `13` rooms produce room sizes `12x21` and `1x20`.</action>
  <verify>npm run test -- tests/api/allocations-route.test.ts</verify>
  <acceptance_criteria>
    - route.ts exports `POST`
    - route.ts returns `400` for malformed payloads
    - route.ts returns `422` for impossible room-count requests
    - allocations-route.test.ts contains two successful requests with identical assignment assertions
    - allocations-route.test.ts asserts the K19A baseline distribution (`272/13` => `12x21`, `1x20`)
    - allocations-route.test.ts contains `Worksheet 1`
    - allocations-route.test.ts contains `/api/allocations`
  </acceptance_criteria>
  <done>The app has a server-owned, persisted allocation endpoint with regression coverage for validation and reproducibility.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npm run test -- tests/api/allocations-route.test.ts passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The repo exposes a persisted allocation API route that returns saved run data
- The same canonical roster and settings produce the same assignment content on repeated requests
</success_criteria>

<threat_model>
<trust_boundaries>
- Browser JSON request -> Next.js allocation route
- Allocation service output -> persisted AllocationRun record
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>02-03-T1</threat_id>
    <category>Tampering</category>
    <component>Allocation request payload</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Validate the full request body with Zod, accept only the three exact strategy keys, reject impossible room counts with HTTP 422, and compute a server-side roster fingerprint instead of trusting any client-supplied metadata.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>02-03-T2</threat_id>
    <category>Integrity</category>
    <component>Saved AllocationRun payload</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Persist the exact engine output and summary fields returned by `createAllocationRun` so the UI and later phases render the authoritative saved snapshot rather than recomputing or reshaping core room assignments ad hoc.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/02-allocation-engine/02-03-SUMMARY.md`
</output>
