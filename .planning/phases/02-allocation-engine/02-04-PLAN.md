---
phase: 02-allocation-engine
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - src/app/page.tsx
  - src/features/roster/ui/import-state.ts
  - src/features/roster/ui/upload-panel.tsx
  - src/features/allocation/ui/allocation-workspace.tsx
  - src/features/allocation/ui/allocation-form.tsx
  - src/features/allocation/ui/allocation-result-summary.tsx
  - src/features/allocation/ui/allocation-room-table.tsx
  - tests/ui/allocation-workspace.test.tsx
autonomous: true
requirements:
  - ALOC-01
  - ALOC-02
  - ALOC-03
  - ALOC-04
  - ALOC-06
user_setup: []
must_haves:
  truths:
    - "A successful import unlocks room-count and strategy selection without inventing client-side allocation results."
    - "The homepage can trigger allocation runs and render the saved server response directly."
    - "Per-room tables expose room sizes and candidate numbers from the authoritative saved run payload."
  artifacts:
    - path: src/features/allocation/ui/allocation-form.tsx
      provides: "Room-count and strategy selection UI"
      contains: "representative_ratio"
    - path: src/features/allocation/ui/allocation-room-table.tsx
      provides: "Per-room allocation result table with candidate numbers"
      contains: "candidateNumber"
    - path: tests/ui/allocation-workspace.test.tsx
      provides: "Component coverage for import-to-allocation interaction"
      contains: "/api/allocations"
  key_links:
    - from: src/features/allocation/ui/allocation-workspace.tsx
      to: src/features/roster/ui/upload-panel.tsx
      via: "Workspace lifts import results so allocation form can reuse canonical students"
      pattern: "onImportResultChange"
    - from: src/features/allocation/ui/allocation-form.tsx
      to: src/app/api/allocations/route.ts
      via: "UI triggers persisted allocation runs via POST"
      pattern: "/api/allocations"
---

<objective>
Extend the operator workflow from successful import into a real room-allocation run UI.

Purpose: Phase 2 is only complete when an operator can choose room count and strategy after import, run the allocator, and inspect the saved room assignments on the homepage.
Output: Lifted import state, allocation form, saved-run result components, page integration, and UI coverage for the import-to-allocation flow.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-allocation-engine/02-RESEARCH.md
@.planning/phases/02-allocation-engine/02-03-SUMMARY.md
@.planning/phases/01-data-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift import state into a shared workspace and expose canonical import results</name>
  <files>src/features/roster/ui/import-state.ts, src/features/roster/ui/upload-panel.tsx, src/features/allocation/ui/allocation-workspace.tsx, src/app/page.tsx</files>
  <read_first>src/app/page.tsx, src/features/roster/ui/import-state.ts, src/features/roster/ui/upload-panel.tsx, src/features/roster/ui/import-summary.tsx, src/features/roster/ui/import-preview-table.tsx, src/features/roster/ui/import-issues-table.tsx</read_first>
  <action>Create `src/features/allocation/ui/allocation-workspace.tsx` as a client component that owns both the import result and the allocation result state. Update `src/features/roster/ui/upload-panel.tsx` to accept an `onImportResultChange` callback and call it whenever a server import payload succeeds or fails, while preserving the existing preview and issue rendering behavior. Update `src/features/roster/ui/import-state.ts` only as needed to export the import payload types cleanly, and update `src/app/page.tsx` to render `AllocationWorkspace` instead of wiring `UploadPanel` directly.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - upload-panel.tsx contains `onImportResultChange`
    - allocation-workspace.tsx contains `useState`
    - allocation-workspace.tsx renders `UploadPanel`
    - src/app/page.tsx renders `AllocationWorkspace`
  </acceptance_criteria>
  <done>The homepage can share one canonical import payload between upload and allocation flows instead of trapping it inside the upload panel.</done>
</task>

<task type="auto">
  <name>Task 2: Build the allocation form and POST workflow for saved runs</name>
  <files>src/features/allocation/ui/allocation-form.tsx, src/features/allocation/ui/allocation-workspace.tsx</files>
  <read_first>.planning/phases/02-allocation-engine/02-RESEARCH.md, src/features/allocation/domain/allocation-types.ts, src/features/allocation/ui/allocation-workspace.tsx, src/features/allocation/ui/allocation-form.tsx</read_first>
  <action>Create `allocation-form.tsx` with a room-count input constrained to `1..99`, explicit strategy controls for `even_mix`, `class_grouped`, and `representative_ratio`, and a submit button that stays disabled until a successful import exists. From `allocation-workspace.tsx`, POST the canonical student payload and selected config to `/api/allocations`, show a pending state while the request runs, and store the saved run response. Keep the browser thin: no client-side room assignment or candidate number generation is allowed. Ensure the request forwards baseline metadata (`sourceFileName`, and `sourceSheetName` when available) from the import payload so saved runs keep traceability to K19A-style inputs.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - allocation-form.tsx contains `even_mix`
    - allocation-form.tsx contains `class_grouped`
    - allocation-form.tsx contains `representative_ratio`
    - allocation-form.tsx contains `min={1}` or `min=\"1\"`
    - allocation-workspace.tsx contains `sourceSheetName`
    - allocation-workspace.tsx contains `fetch('/api/allocations'`
  </acceptance_criteria>
  <done>The operator can trigger saved allocation runs from the homepage with explicit room-count and strategy controls.</done>
</task>

<task type="auto">
  <name>Task 3: Render saved allocation results and add UI coverage for the full interaction</name>
  <files>src/features/allocation/ui/allocation-result-summary.tsx, src/features/allocation/ui/allocation-room-table.tsx, tests/ui/allocation-workspace.test.tsx</files>
  <read_first>src/features/allocation/ui/allocation-workspace.tsx, src/features/allocation/ui/allocation-result-summary.tsx, src/features/allocation/ui/allocation-room-table.tsx, tests/ui/allocation-workspace.test.tsx</read_first>
  <action>Create `allocation-result-summary.tsx` that displays the saved run strategy, total students, room count, room-size spread, and creation timestamp. Create `allocation-room-table.tsx` that renders each room with its room number, room size, student class, `MSHV`, canonical full name, and generated `candidateNumber`. Add `tests/ui/allocation-workspace.test.tsx` that mocks both the import response and the allocation response, verifies the allocation form stays disabled before a successful import, verifies a strategy can be selected and submitted after import, and verifies saved room data such as `P01-001` and room-size counts appear from the server response. Include a K19A-derived test case (`272` students, `13` rooms) asserting the displayed room-size distribution matches `12x21` and `1x20`.</action>
  <verify>npm run test -- tests/ui/allocation-workspace.test.tsx</verify>
  <acceptance_criteria>
    - allocation-result-summary.tsx contains `roomCount`
    - allocation-room-table.tsx contains `candidateNumber`
    - allocation-room-table.tsx contains `MSHV`
    - allocation-workspace.test.tsx contains `P01-001`
    - allocation-workspace.test.tsx asserts the K19A baseline distribution (`272/13` => `12x21`, `1x20`)
    - allocation-workspace.test.tsx asserts the allocation form is disabled before import success
  </acceptance_criteria>
  <done>The homepage shows the authoritative saved allocation result and has regression coverage for the core Phase 2 operator flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npm run test -- tests/ui/allocation-workspace.test.tsx passes
- [ ] npm run build passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The homepage lets an operator choose room count and strategy after a successful import
- The UI renders saved room assignments and candidate numbers from the server response only
</success_criteria>

<threat_model>
<trust_boundaries>
- Upload-derived client state -> allocation request UI
- Saved allocation API response -> rendered room tables and summary cards
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>02-04-T1</threat_id>
    <category>Integrity</category>
    <component>Allocation workspace client state</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Lift the import payload into one workspace component and use it only as the request body for `/api/allocations`. Do not generate or mutate room assignments in the browser; always render the saved server response as the source of truth.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>02-04-T2</threat_id>
    <category>Repudiation</category>
    <component>Operator-visible allocation result</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Render saved run metadata such as strategy, room count, and creation timestamp alongside the room tables so later export, review, and history flows can tie the visible result back to one persisted AllocationRun record.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/02-allocation-engine/02-04-SUMMARY.md`
</output>
