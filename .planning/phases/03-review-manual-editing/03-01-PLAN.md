---
phase: 03-review-manual-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/features/allocation/domain/allocation-types.ts
  - src/features/allocation/domain/project-manual-edits.ts
  - src/features/allocation/domain/build-review-summary.ts
  - src/features/allocation/domain/validate-manual-edits.ts
  - src/features/allocation/server/load-allocation-run.ts
  - src/features/allocation/server/save-manual-edits.ts
  - src/app/api/allocations/[id]/route.ts
  - tests/allocation/build-review-summary.test.ts
  - tests/allocation/validate-manual-edits.test.ts
  - tests/api/allocation-run-route.test.ts
autonomous: true
requirements:
  - PREV-03
  - PREV-04
  - PREV-05
  - EDIT-04
  - EDIT-05
user_setup: []
must_haves:
  truths:
    - "The app preserves the original auto-allocation snapshot while allowing an edited snapshot to become the authoritative preview."
    - "Manual-edit validation distinguishes blocking integrity failures from operator-visible warnings."
    - "Saving edits always returns a server-recomputed snapshot instead of trusting client-sent seat indexes or candidate numbers."
  artifacts:
    - path: prisma/schema.prisma
      provides: "Editable allocation persistence fields and optimistic concurrency metadata"
      contains: "editedResultSnapshot"
    - path: src/app/api/allocations/[id]/route.ts
      provides: "GET/PATCH route for loading and saving a saved allocation run"
      contains: "PATCH"
    - path: src/features/allocation/domain/validate-manual-edits.ts
      provides: "Integrity and warning classification for manual edits"
      contains: "blockingIssues"
  key_links:
    - from: src/features/allocation/server/save-manual-edits.ts
      to: src/features/allocation/domain/build-review-summary.ts
      via: "Server save path recomputes warnings and fairness metrics from edited room ordering"
      pattern: "buildReviewSummary"
    - from: src/app/api/allocations/[id]/route.ts
      to: src/features/allocation/server/load-allocation-run.ts
      via: "Route resolves edited snapshot when present"
      pattern: "loadAllocationRun"
---

<objective>
Create the Phase 3 backend and shared-domain foundation for editable saved runs.

Purpose: Manual editing is only safe if the server can load one authoritative run, validate draft edits, recompute summary metrics and warnings, and persist an edited snapshot without destroying the original Phase 2 allocation result.
Output: Prisma schema extensions, shared edit-projection utilities, GET/PATCH allocation-run route, and domain/API tests for edit integrity and optimistic concurrency.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-review-manual-editing/03-RESEARCH.md
@.planning/phases/02-allocation-engine/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the saved-run contract and Prisma schema for editable authoritative previews</name>
  <files>prisma/schema.prisma, src/features/allocation/domain/allocation-types.ts</files>
  <read_first>prisma/schema.prisma, src/features/allocation/domain/allocation-types.ts, src/features/allocation/server/save-allocation-run.ts, src/features/allocation/server/run-allocation.ts</read_first>
  <action>Extend `AllocationRun` in `prisma/schema.prisma` with optional `editedResultSnapshot Json?`, optional `editedSummary Json?`, `editVersion Int @default(0)`, `lastEditedAt DateTime?`, and `updatedAt DateTime @updatedAt`. Extend `src/features/allocation/domain/allocation-types.ts` with concrete review/edit contracts: `AllocationWarningSeverity`, `AllocationWarning`, `RoomSizeBucket`, `RoomClassBreakdown`, `ClassDistributionMetric`, `ReviewSummary`, `EditableAllocationRun`, `ManualEditRoomPayload`, and `expectedEditVersion` support on the save payload. Preserve the existing Phase 2 fields so the initial POST `/api/allocations` contract remains backward compatible.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `editedResultSnapshot`
    - prisma/schema.prisma contains `editedSummary`
    - prisma/schema.prisma contains `editVersion`
    - prisma/schema.prisma contains `updatedAt`
    - src/features/allocation/domain/allocation-types.ts contains `AllocationWarning`
    - src/features/allocation/domain/allocation-types.ts contains `EditableAllocationRun`
  </acceptance_criteria>
  <done>The persistence and TypeScript contracts can represent original snapshots, edited snapshots, warnings, fairness metrics, and optimistic concurrency metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Build shared projection, summary, and validation utilities for manual edits</name>
  <files>src/features/allocation/domain/project-manual-edits.ts, src/features/allocation/domain/build-review-summary.ts, src/features/allocation/domain/validate-manual-edits.ts, tests/allocation/build-review-summary.test.ts, tests/allocation/validate-manual-edits.test.ts</files>
  <read_first>src/features/allocation/domain/create-allocation-run.ts, src/features/allocation/domain/generate-candidate-numbers.ts, src/features/allocation/domain/validate-allocation-result.ts, src/features/allocation/domain/allocation-types.ts, tests/allocation/create-allocation-run.test.ts</read_first>
  <action>Create `project-manual-edits.ts` to rebuild room assignments from ordered student keys using the persisted canonical roster, then regenerate `seatIndex` and `candidateNumber` deterministically from room number plus position. Create `build-review-summary.ts` to compute total students, room count, size spread, room-size buckets, per-room class counts, per-class room coverage, and warning records. Create `validate-manual-edits.ts` to return explicit `blockingIssues` and `warningIssues`; blocking must cover missing students, duplicate students, unknown student keys, and impossible room references, while warning severity must include `room size spread is greater than 1`. Add focused unit tests covering duplicate detection, missing-student detection, candidate-number regeneration after reorder, and room-size warning generation.</action>
  <verify>npm run test -- tests/allocation/build-review-summary.test.ts tests/allocation/validate-manual-edits.test.ts</verify>
  <acceptance_criteria>
    - src/features/allocation/domain/project-manual-edits.ts contains `candidateNumber`
    - src/features/allocation/domain/build-review-summary.ts contains `classDistribution`
    - src/features/allocation/domain/validate-manual-edits.ts contains `blockingIssues`
    - src/features/allocation/domain/validate-manual-edits.ts contains `warningIssues`
    - tests/allocation/validate-manual-edits.test.ts contains `missing student`
    - tests/allocation/build-review-summary.test.ts contains `room size spread is greater than 1`
  </acceptance_criteria>
  <done>The project has one shared source of truth for recomputing draft previews and validating edits before persistence.</done>
</task>

<task type="auto">
  <name>Task 3: Add load/save services and a GET/PATCH route for authoritative edited runs</name>
  <files>src/features/allocation/server/load-allocation-run.ts, src/features/allocation/server/save-manual-edits.ts, src/app/api/allocations/[id]/route.ts, tests/api/allocation-run-route.test.ts</files>
  <read_first>src/app/api/allocations/route.ts, src/features/allocation/server/save-allocation-run.ts, src/features/allocation/domain/allocation-types.ts, src/features/allocation/domain/project-manual-edits.ts, src/features/allocation/domain/build-review-summary.ts, src/features/allocation/domain/validate-manual-edits.ts, tests/api/allocations-route.test.ts</read_first>
  <action>Create `load-allocation-run.ts` to read an `AllocationRun` and resolve the authoritative preview via `editedResultSnapshot ?? resultSnapshot` and `editedSummary ?? summary`. Create `save-manual-edits.ts` to accept `{ allocationRunId, expectedEditVersion, rooms }`, reject stale versions with `409`, rebuild the edited snapshot with the shared domain utilities, persist `editedResultSnapshot`, `editedSummary`, incremented `editVersion`, and `lastEditedAt`, then return the resolved authoritative run. Create `src/app/api/allocations/[id]/route.ts` with `GET` for loading the resolved run and `PATCH` for saving edits. Add API tests covering successful save, stale edit-version rejection, malformed payload rejection, and GET resolution of an already edited run.</action>
  <verify>npm run test -- tests/api/allocation-run-route.test.ts</verify>
  <acceptance_criteria>
    - src/app/api/allocations/[id]/route.ts contains `export async function GET`
    - src/app/api/allocations/[id]/route.ts contains `export async function PATCH`
    - src/features/allocation/server/save-manual-edits.ts contains `expectedEditVersion`
    - src/features/allocation/server/save-manual-edits.ts contains `409`
    - tests/api/allocation-run-route.test.ts contains `stale`
    - tests/api/allocation-run-route.test.ts contains `editedResultSnapshot`
  </acceptance_criteria>
  <done>The workspace can load an authoritative saved run and persist edited snapshots through a server-owned contract.</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Push the Prisma schema before verification</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma, package.json</read_first>
  <action>After the schema changes from Task 1 are complete, run `npx prisma db push` before verification. If the command reports that the live database schema is not aligned with the updated `AllocationRun` model, do not continue to typecheck or API tests until the push succeeds. Treat this as a blocking task because TypeScript can pass while the database stays out of sync.</action>
  <verify>npx prisma db push</verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `editedResultSnapshot`
    - prisma/schema.prisma contains `editVersion`
    - `npx prisma db push` exits 0
  </acceptance_criteria>
  <done>The live database schema matches the editable saved-run contract before any Phase 3 verification is trusted.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx prisma db push passes
- [ ] npm run typecheck passes
- [ ] npm run test -- tests/allocation/build-review-summary.test.ts tests/allocation/validate-manual-edits.test.ts tests/api/allocation-run-route.test.ts passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Saved runs can be loaded with one authoritative preview contract
- Manual edits can be validated and saved without losing the original Phase 2 allocation snapshot
</success_criteria>

<threat_model>
<trust_boundaries>
- Client edit draft payload -> server save route
- Prisma AllocationRun row -> authoritative preview resolution
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>03-01-T1</threat_id>
    <category>Integrity</category>
    <component>Manual edit save payload</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Never trust client-sent candidate numbers or seat indexes. Rebuild room ordering from stable student keys, then regenerate seat indexes, candidate numbers, warnings, and summary metrics on the server before persistence.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>03-01-T2</threat_id>
    <category>Tampering</category>
    <component>Concurrent manual edits</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Persist `editVersion` and require `expectedEditVersion` on PATCH so stale clients receive `409` instead of silently overwriting a newer authoritative preview.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>03-01-T3</threat_id>
    <category>Repudiation</category>
    <component>Edited snapshot provenance</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Preserve the original auto-allocation `resultSnapshot` and `summary`, and store edited state separately so the system can still explain what the auto-allocation produced versus what the operator approved later.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/03-review-manual-editing/03-01-SUMMARY.md`
</output>
