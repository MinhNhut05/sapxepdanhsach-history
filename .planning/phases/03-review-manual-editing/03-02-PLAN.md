---
phase: 03-review-manual-editing
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/features/allocation/ui/allocation-workspace.tsx
  - src/features/allocation/ui/allocation-result-summary.tsx
  - src/features/allocation/ui/allocation-room-table.tsx
  - src/features/allocation/ui/allocation-warning-panel.tsx
  - src/features/allocation/ui/allocation-fairness-matrix.tsx
  - tests/ui/allocation-review-dashboard.test.tsx
autonomous: true
requirements:
  - PREV-01
  - PREV-02
  - PREV-03
  - PREV-04
  - PREV-05
user_setup: []
must_haves:
  truths:
    - "The preview renders the authoritative saved run, including edited snapshots when they exist."
    - "Operators can see room-size, class-distribution, and warning information before touching export or print flows."
    - "Review UI keeps import and allocation warnings visible instead of burying them inside API payloads."
  artifacts:
    - path: src/features/allocation/ui/allocation-warning-panel.tsx
      provides: "Visible warning surfacing for import and allocation issues"
      contains: "warning"
    - path: src/features/allocation/ui/allocation-fairness-matrix.tsx
      provides: "Per-room and per-class fairness breakdown"
      contains: "classDistribution"
    - path: tests/ui/allocation-review-dashboard.test.tsx
      provides: "UI coverage for the review dashboard"
      contains: "Độ lệch sĩ số"
  key_links:
    - from: src/features/allocation/ui/allocation-workspace.tsx
      to: src/features/allocation/ui/allocation-warning-panel.tsx
      via: "Workspace feeds authoritative warning data into the review dashboard"
      pattern: "AllocationWarningPanel"
    - from: src/features/allocation/ui/allocation-workspace.tsx
      to: src/features/allocation/ui/allocation-fairness-matrix.tsx
      via: "Workspace passes resolved fairness metrics into the preview"
      pattern: "AllocationFairnessMatrix"
---

<objective>
Turn the Phase 2 saved-run preview into a real review dashboard.

Purpose: Operators need a single screen that shows the authoritative room assignment, room-size balance, class-distribution information, and warnings before they start editing or approving the result.
Output: Warning panel, fairness matrix, upgraded summary/table components, and review-dashboard UI coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-review-manual-editing/03-RESEARCH.md
@.planning/phases/03-review-manual-editing/03-01-PLAN.md
@.planning/phases/02-allocation-engine/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit warning and fairness components for review-mode visibility</name>
  <files>src/features/allocation/ui/allocation-warning-panel.tsx, src/features/allocation/ui/allocation-fairness-matrix.tsx</files>
  <read_first>src/features/allocation/domain/allocation-types.ts, src/features/allocation/ui/allocation-result-summary.tsx, src/features/allocation/ui/allocation-room-table.tsx, src/app/globals.css</read_first>
  <action>Create `allocation-warning-panel.tsx` to render both blocking and warning issues from the authoritative saved run, grouped by severity with clear operator wording. Create `allocation-fairness-matrix.tsx` to show room-size buckets, per-room class counts, per-class room coverage, and dominant-class percentages using badges/tables rather than charts. Use the existing visual language from `src/app/globals.css`; do not introduce a charting library.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/features/allocation/ui/allocation-warning-panel.tsx contains `blocking`
    - src/features/allocation/ui/allocation-warning-panel.tsx contains `warning`
    - src/features/allocation/ui/allocation-fairness-matrix.tsx contains `classDistribution`
    - src/features/allocation/ui/allocation-fairness-matrix.tsx contains `dominant`
  </acceptance_criteria>
  <done>The review screen has dedicated components for fairness and warning visibility instead of overloading the Phase 2 summary card.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade the saved-run summary and room preview for review-first workflows</name>
  <files>src/features/allocation/ui/allocation-result-summary.tsx, src/features/allocation/ui/allocation-room-table.tsx</files>
  <read_first>src/features/allocation/ui/allocation-result-summary.tsx, src/features/allocation/ui/allocation-room-table.tsx, src/features/allocation/domain/allocation-types.ts</read_first>
  <action>Update `allocation-result-summary.tsx` so it renders authoritative review fields beyond the Phase 2 baseline: `editVersion`, edited timestamp when present, room-size histogram badges, and a summary badge indicating whether the preview is the original auto-allocation result or an edited snapshot. Update `allocation-room-table.tsx` to expose room order, room size, class distribution context, and any room-level warnings while preserving the existing per-student columns (`SBD`, `Lớp`, `MSHV`, `Họ tên`).</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/features/allocation/ui/allocation-result-summary.tsx contains `editVersion`
    - src/features/allocation/ui/allocation-result-summary.tsx contains `edited`
    - src/features/allocation/ui/allocation-room-table.tsx contains `roomNumber`
    - src/features/allocation/ui/allocation-room-table.tsx contains `candidateNumber`
  </acceptance_criteria>
  <done>The authoritative preview explains both the saved-run identity and the review status that operators need before making changes.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate the review dashboard into the homepage workspace and add focused UI coverage</name>
  <files>src/features/allocation/ui/allocation-workspace.tsx, tests/ui/allocation-review-dashboard.test.tsx</files>
  <read_first>src/features/allocation/ui/allocation-workspace.tsx, src/features/allocation/ui/allocation-warning-panel.tsx, src/features/allocation/ui/allocation-fairness-matrix.tsx, src/features/allocation/ui/allocation-result-summary.tsx, src/features/allocation/ui/allocation-room-table.tsx, tests/ui/allocation-workspace.test.tsx</read_first>
  <action>Update `allocation-workspace.tsx` so a successful saved run renders a cohesive review dashboard: summary first, warnings second, fairness matrix third, and room preview last. Preserve the Phase 2 import-to-allocation flow, but carry import issues forward so they remain visible after allocation. Add `tests/ui/allocation-review-dashboard.test.tsx` that renders an authoritative run with warnings and class-distribution metrics, then asserts the dashboard shows room-size spread, fairness rows, warning severity labels, and per-room student previews before edit mode is entered.</action>
  <verify>npm run test -- tests/ui/allocation-review-dashboard.test.tsx</verify>
  <acceptance_criteria>
    - src/features/allocation/ui/allocation-workspace.tsx contains `AllocationWarningPanel`
    - src/features/allocation/ui/allocation-workspace.tsx contains `AllocationFairnessMatrix`
    - tests/ui/allocation-review-dashboard.test.tsx contains `Độ lệch sĩ số`
    - tests/ui/allocation-review-dashboard.test.tsx contains `warning`
    - tests/ui/allocation-review-dashboard.test.tsx contains `classDistribution`
  </acceptance_criteria>
  <done>The homepage preview is now a review dashboard that lets operators judge fairness and warnings before they edit or approve the run.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npm run test -- tests/ui/allocation-review-dashboard.test.tsx passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Operators can review the authoritative saved run with fairness and warning visibility before editing
</success_criteria>

<threat_model>
<trust_boundaries>
- Authoritative saved-run payload -> review dashboard
- Import issues -> post-allocation operator warnings
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>03-02-T1</threat_id>
    <category>Integrity</category>
    <component>Review dashboard rendering</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Render fairness metrics and warnings from the authoritative server-owned saved-run payload, not from ad-hoc client calculations detached from the persisted snapshot.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>03-02-T2</threat_id>
    <category>Repudiation</category>
    <component>Operator review context</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Show run identity, edit version, and edited/original status in the summary so staff can tell what result they are reviewing before later export and print flows depend on it.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/03-review-manual-editing/03-02-SUMMARY.md`
</output>
