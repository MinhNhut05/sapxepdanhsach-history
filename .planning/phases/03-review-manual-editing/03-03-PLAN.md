---
phase: 03-review-manual-editing
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - package.json
  - package-lock.json
  - src/features/allocation/ui/use-allocation-draft.ts
  - src/features/allocation/ui/allocation-editor.tsx
  - src/features/allocation/ui/editable-room-column.tsx
  - src/features/allocation/ui/allocation-workspace.tsx
  - tests/ui/allocation-editor.test.tsx
autonomous: true
requirements:
  - EDIT-01
  - EDIT-02
  - EDIT-03
  - EDIT-04
user_setup: []
must_haves:
  truths:
    - "Operators can move students between rooms, reorder students within a room, and use drag-and-drop without losing roster integrity."
    - "Draft feedback updates immediately from shared projection logic, but unsaved client state is never mistaken for the authoritative preview."
    - "Blocking integrity issues prevent save while still letting the operator inspect warning-only states."
  artifacts:
    - path: package.json
      provides: "Drag-and-drop dependencies for React-based room editing"
      contains: "@dnd-kit/core"
    - path: src/features/allocation/ui/use-allocation-draft.ts
      provides: "Normalized draft-state controller for move and reorder operations"
      contains: "moveStudent"
    - path: src/features/allocation/ui/allocation-editor.tsx
      provides: "Operator editing interface with room-to-room movement"
      contains: "DndContext"
  key_links:
    - from: src/features/allocation/ui/use-allocation-draft.ts
      to: src/features/allocation/domain/project-manual-edits.ts
      via: "Draft state reprojects provisional rooms and candidate numbers through shared domain logic"
      pattern: "projectManualEdits"
    - from: src/features/allocation/ui/allocation-editor.tsx
      to: src/features/allocation/ui/editable-room-column.tsx
      via: "Editor renders one sortable room column per room"
      pattern: "EditableRoomColumn"
---

<objective>
Deliver the manual-editing interaction layer on top of the authoritative Phase 3 contracts.

Purpose: The review dashboard is not enough unless operators can reorder students within rooms, move them between rooms, and drag-and-drop safely with immediate validation feedback.
Output: DnD dependencies, normalized draft-state hook, editable room board, and UI tests for move/reorder behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-review-manual-editing/03-RESEARCH.md
@.planning/phases/03-review-manual-editing/03-01-PLAN.md
@.planning/phases/02-allocation-engine/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install drag-and-drop dependencies and build normalized draft-state control</name>
  <files>package.json, package-lock.json, src/features/allocation/ui/use-allocation-draft.ts</files>
  <read_first>package.json, src/features/allocation/domain/allocation-types.ts, src/features/allocation/domain/project-manual-edits.ts, src/features/allocation/domain/validate-manual-edits.ts, src/features/allocation/ui/allocation-workspace.tsx</read_first>
  <action>Install `@dnd-kit/core`, `@dnd-kit/sortable`, and `@dnd-kit/utilities`. Create `use-allocation-draft.ts` that seeds from the authoritative saved run and exposes normalized draft operations: `moveStudent`, `reorderStudent`, `resetDraft`, `dirty`, `blockingIssues`, `warningIssues`, and the projected draft rooms with regenerated candidate numbers. Use stable student identity based on canonical `studentCode` plus `rowIndex`, not array indexes, so cross-room moves stay deterministic.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - package.json contains `@dnd-kit/core`
    - package.json contains `@dnd-kit/sortable`
    - src/features/allocation/ui/use-allocation-draft.ts contains `moveStudent`
    - src/features/allocation/ui/use-allocation-draft.ts contains `resetDraft`
    - src/features/allocation/ui/use-allocation-draft.ts contains `blockingIssues`
  </acceptance_criteria>
  <done>The editor has one reusable state controller that understands room moves, reorders, and validation state.</done>
</task>

<task type="auto">
  <name>Task 2: Build the editable room board with drag-and-drop and explicit move controls</name>
  <files>src/features/allocation/ui/allocation-editor.tsx, src/features/allocation/ui/editable-room-column.tsx</files>
  <read_first>src/features/allocation/ui/use-allocation-draft.ts, src/features/allocation/ui/allocation-room-table.tsx, src/app/globals.css</read_first>
  <action>Create `allocation-editor.tsx` using `DndContext`, sortable room lists, and per-student drag handles. Create `editable-room-column.tsx` to render each room as a sortable column showing provisional `candidateNumber`, class name, `MSHV`, and room size. Include explicit accessible controls for `move up`, `move down`, and `move to next/previous room` alongside drag handles so keyboard users and tests can trigger the same edit operations deterministically.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/features/allocation/ui/allocation-editor.tsx contains `DndContext`
    - src/features/allocation/ui/allocation-editor.tsx contains `SortableContext`
    - src/features/allocation/ui/editable-room-column.tsx contains `move up`
    - src/features/allocation/ui/editable-room-column.tsx contains `candidateNumber`
  </acceptance_criteria>
  <done>The UI exposes both production drag-and-drop and reliable explicit controls for room-to-room and in-room editing.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate draft editing into the workspace and add interaction coverage</name>
  <files>src/features/allocation/ui/allocation-workspace.tsx, tests/ui/allocation-editor.test.tsx</files>
  <read_first>src/features/allocation/ui/allocation-workspace.tsx, src/features/allocation/ui/use-allocation-draft.ts, src/features/allocation/ui/allocation-editor.tsx, tests/ui/allocation-workspace.test.tsx</read_first>
  <action>Update `allocation-workspace.tsx` so that once a saved run exists, the page offers an edit mode backed by `use-allocation-draft`. Show dirty-state messaging, inline validation counts, and a reset action that restores the current authoritative saved run. Add `tests/ui/allocation-editor.test.tsx` covering at least three cases: reordering within one room updates provisional candidate numbers, moving a student to another room updates room counts, and a size-spread violation surfaces warning feedback without deleting any student rows.</action>
  <verify>npm run test -- tests/ui/allocation-editor.test.tsx</verify>
  <acceptance_criteria>
    - src/features/allocation/ui/allocation-workspace.tsx contains `dirty`
    - src/features/allocation/ui/allocation-workspace.tsx contains `resetDraft`
    - tests/ui/allocation-editor.test.tsx contains `moveStudent`
    - tests/ui/allocation-editor.test.tsx contains `candidateNumber`
    - tests/ui/allocation-editor.test.tsx contains `size spread`
  </acceptance_criteria>
  <done>The homepage supports safe provisional editing with immediate feedback before anything is persisted.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npm run test -- tests/ui/allocation-editor.test.tsx passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Operators can reorder and move students safely with immediate feedback and drag-and-drop support
</success_criteria>

<threat_model>
<trust_boundaries>
- User drag/drop interaction -> client draft state
- Client draft state -> projected preview and validation feedback
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>03-03-T1</threat_id>
    <category>Integrity</category>
    <component>Draft room editing state</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use stable student keys and normalized room collections instead of positional array identity so drag/drop and move buttons cannot silently duplicate or drop students during reordering.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>03-03-T2</threat_id>
    <category>Availability</category>
    <component>Editing UX</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Provide explicit accessible move controls in addition to drag handles so editing remains possible when pointer drag interaction or test environments are limited.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/03-review-manual-editing/03-03-SUMMARY.md`
</output>
