---
phase: 03-review-manual-editing
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - src/features/allocation/ui/allocation-workspace.tsx
  - src/features/allocation/ui/allocation-save-bar.tsx
  - src/app/api/allocations/[id]/route.ts
  - src/features/allocation/server/save-manual-edits.ts
  - tests/ui/allocation-workspace.test.tsx
  - tests/api/allocation-run-route.test.ts
autonomous: true
requirements:
  - PREV-01
  - PREV-05
  - EDIT-04
  - EDIT-05
user_setup: []
must_haves:
  truths:
    - "Unsaved draft edits are clearly separate from the authoritative preview until the server accepts them."
    - "Saving edits replaces the workspace state with the server-returned authoritative snapshot so later export/print work can trust one source of truth."
    - "Conflicts and validation failures surface clearly instead of silently discarding or overwriting edits."
  artifacts:
    - path: src/features/allocation/ui/allocation-save-bar.tsx
      provides: "Save/reset/conflict UI for manual editing"
      contains: "Lưu chỉnh sửa"
    - path: src/features/allocation/ui/allocation-workspace.tsx
      provides: "PATCH workflow that rehydrates the authoritative preview from the server response"
      contains: "/api/allocations/"
    - path: tests/ui/allocation-workspace.test.tsx
      provides: "Regression coverage for edit-save-preview synchronization"
      contains: "expectedEditVersion"
  key_links:
    - from: src/features/allocation/ui/allocation-workspace.tsx
      to: src/app/api/allocations/[id]/route.ts
      via: "Workspace persists edits through the server and replaces draft state with the authoritative response"
      pattern: "PATCH"
    - from: src/features/allocation/ui/allocation-save-bar.tsx
      to: src/features/allocation/ui/use-allocation-draft.ts
      via: "Save bar is driven by dirty state and validation counts"
      pattern: "blockingIssues"
---

<objective>
Close Phase 3 by wiring save/sync behavior and full review-to-edit regression coverage.

Purpose: The phase is only complete once operators can save manual edits, receive the authoritative server-owned preview back, and trust that preview as the single source of truth for later output phases.
Output: Save bar, PATCH integration, conflict/error handling, and regression tests for edit-save-preview synchronization.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-review-manual-editing/03-RESEARCH.md
@.planning/phases/03-review-manual-editing/03-02-PLAN.md
@.planning/phases/03-review-manual-editing/03-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add save/reset/conflict UI for manual edits</name>
  <files>src/features/allocation/ui/allocation-save-bar.tsx, src/features/allocation/ui/allocation-workspace.tsx</files>
  <read_first>src/features/allocation/ui/allocation-workspace.tsx, src/features/allocation/ui/use-allocation-draft.ts, src/features/allocation/domain/allocation-types.ts, src/app/globals.css</read_first>
  <action>Create `allocation-save-bar.tsx` to show dirty-state messaging, blocking/warning counts, `Lưu chỉnh sửa`, `Đặt lại`, and stale/conflict messaging. Update `allocation-workspace.tsx` to render the save bar only when a saved run exists and edit mode is active. Keep the authoritative review dashboard visible, but label provisional draft state clearly so operators can tell whether they are viewing unsaved edits or the last persisted result.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/features/allocation/ui/allocation-save-bar.tsx contains `Lưu chỉnh sửa`
    - src/features/allocation/ui/allocation-save-bar.tsx contains `Đặt lại`
    - src/features/allocation/ui/allocation-workspace.tsx contains `draft`
    - src/features/allocation/ui/allocation-workspace.tsx contains `authoritative`
  </acceptance_criteria>
  <done>The workspace makes it obvious when the operator has unsaved changes and what actions are available next.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PATCH save flow and authoritative rehydration</name>
  <files>src/features/allocation/ui/allocation-workspace.tsx, src/app/api/allocations/[id]/route.ts, src/features/allocation/server/save-manual-edits.ts</files>
  <read_first>src/features/allocation/ui/allocation-workspace.tsx, src/features/allocation/ui/use-allocation-draft.ts, src/app/api/allocations/[id]/route.ts, src/features/allocation/server/save-manual-edits.ts</read_first>
  <action>Update `allocation-workspace.tsx` to PATCH `/api/allocations/{id}` with `{ expectedEditVersion, rooms }` from the draft state, surface `409` stale-version conflicts distinctly, and on success replace the workspace `savedRun` with the authoritative server response while resetting draft dirty state. Ensure the save action is disabled when `blockingIssues.length > 0`. If the PATCH fails, keep the draft intact and show an error that distinguishes validation failure, stale conflict, and generic persistence failure.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/features/allocation/ui/allocation-workspace.tsx contains `expectedEditVersion`
    - src/features/allocation/ui/allocation-workspace.tsx contains `PATCH`
    - src/features/allocation/ui/allocation-workspace.tsx contains `409`
    - src/features/allocation/ui/allocation-workspace.tsx contains `blockingIssues.length`
  </acceptance_criteria>
  <done>Saving edits produces a new authoritative preview in the workspace instead of leaving the client and server out of sync.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for edit-save-preview synchronization</name>
  <files>tests/ui/allocation-workspace.test.tsx, tests/api/allocation-run-route.test.ts</files>
  <read_first>tests/ui/allocation-workspace.test.tsx, tests/api/allocation-run-route.test.ts, src/features/allocation/ui/allocation-workspace.tsx, src/app/api/allocations/[id]/route.ts</read_first>
  <action>Extend `tests/ui/allocation-workspace.test.tsx` to cover the full flow: import -> auto-allocation -> enter edit mode -> move/reorder a student -> save via PATCH -> render the authoritative edited snapshot from the server response. Add regression assertions for disabled save when blocking issues exist, retention of draft state on PATCH failure, and stale-version conflict handling. Extend `tests/api/allocation-run-route.test.ts` so the saved response proves that `editedResultSnapshot` is what the client receives after a successful PATCH.</action>
  <verify>npm run test -- tests/ui/allocation-workspace.test.tsx tests/api/allocation-run-route.test.ts</verify>
  <acceptance_criteria>
    - tests/ui/allocation-workspace.test.tsx contains `expectedEditVersion`
    - tests/ui/allocation-workspace.test.tsx contains `PATCH`
    - tests/ui/allocation-workspace.test.tsx contains `409`
    - tests/api/allocation-run-route.test.ts contains `editedResultSnapshot`
    - tests/ui/allocation-workspace.test.tsx contains `edited snapshot`
  </acceptance_criteria>
  <done>The phase has end-to-end proof that saved edits drive the authoritative preview the rest of the product will consume.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run typecheck passes
- [ ] npm run test -- tests/ui/allocation-workspace.test.tsx tests/api/allocation-run-route.test.ts passes
- [ ] npm run build passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Operators can save manual edits and immediately see the authoritative edited preview returned by the server
- The workspace clearly separates unsaved draft state from the authoritative preview
</success_criteria>

<threat_model>
<trust_boundaries>
- Draft edit state -> PATCH save request
- PATCH response -> authoritative preview shown to the operator
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>03-04-T1</threat_id>
    <category>Integrity</category>
    <component>Review/edit synchronization</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>After a successful PATCH, replace local saved-run state with the full server response and reset draft dirty state so the browser never continues to show stale pre-save data as the authoritative preview.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>03-04-T2</threat_id>
    <category>Tampering</category>
    <component>Stale edit overwrites</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Surface `409` conflicts clearly in the save UI and keep the draft intact so the operator can decide how to reconcile instead of losing work or silently overwriting newer edits.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/03-review-manual-editing/03-04-SUMMARY.md`
</output>
