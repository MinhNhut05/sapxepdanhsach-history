---
phase: 04-output-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/allocation/domain/allocation-types.ts
  - src/features/allocation/server/project-output-records.ts
  - src/features/allocation/server/history-retention.ts
  - src/features/allocation/server/list-allocation-runs.ts
  - src/features/allocation/server/load-allocation-run.ts
  - src/app/api/allocations/route.ts
  - tests/allocation/project-output-records.test.ts
  - tests/api/allocations-route.test.ts
autonomous: true
requirements:
  - EXPT-04
  - EXPT-06
  - HIST-02
  - SAFE-03
user_setup: []
must_haves:
  truths:
    - "Every later output flow reads one server-owned authoritative projection instead of duplicating room-row logic in separate export and print paths."
    - "Saved-run history and reopen flows only expose runs that are still inside the configured retention policy."
    - "The history list returns enough metadata for operators to reopen the right run without re-importing a roster."
  artifacts:
    - path: src/features/allocation/server/project-output-records.ts
      provides: "Authoritative flattened output rows for export and print"
      contains: "candidateNumber"
    - path: src/features/allocation/server/history-retention.ts
      provides: "Retention-policy evaluation and pruning helpers"
      contains: "ALLOCATION_RUN_RETENTION_DAYS"
    - path: src/app/api/allocations/route.ts
      provides: "Saved-run history listing alongside the existing create route"
      contains: "export async function GET"
  key_links:
    - from: src/features/allocation/server/list-allocation-runs.ts
      to: src/features/allocation/server/history-retention.ts
      via: "History listing filters and prunes expired runs before returning metadata to the UI"
      pattern: "pruneExpiredAllocationRuns"
    - from: src/features/allocation/server/project-output-records.ts
      to: src/features/allocation/server/load-allocation-run.ts
      via: "Output rows are derived from the same authoritative edited-or-original snapshot resolution used for reopening"
      pattern: "loadAllocationRun"
---

<objective>
Establish the Phase 4 server foundation for authoritative outputs, retention-aware history, and safe reopening.

Purpose: Excel export, print, and reopen flows will drift or leak expired data unless they all share one server-owned output projection and one retention policy.
Output: shared output-row projector, retention helpers, history-list service, hardened load behavior, and API/test coverage for reopen metadata.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-output-operations/04-RESEARCH.md
@.planning/phases/03-review-manual-editing/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Phase 3 saved-run contracts with output-row and history-list types</name>
  <files>src/features/allocation/domain/allocation-types.ts</files>
  <read_first>src/features/allocation/domain/allocation-types.ts, src/features/allocation/server/load-allocation-run.ts, src/features/allocation/ui/allocation-result-summary.tsx, src/features/allocation/ui/allocation-room-table.tsx</read_first>
  <action>Add concrete Phase 4 types to `src/features/allocation/domain/allocation-types.ts` for `AllocationOutputRow`, `AllocationHistoryItem`, `AllocationRetentionPolicy`, and `AllocationHistoryResponse`. `AllocationOutputRow` must expose exact fields for output generation: `roomLabel`, `roomNumber`, `candidateNumber`, `seatIndex`, `className`, `studentCode`, `middleName`, `firstName`, `fullName`, `birthDateIso`, `birthPlace`, and `note`. `AllocationHistoryItem` must expose `id`, `sourceFileName`, `createdAt`, `lastEditedAt`, `strategy`, `roomCount`, `totalStudents`, and `isEdited` so the homepage can reopen a run without additional shape inference.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/features/allocation/domain/allocation-types.ts contains `export interface AllocationOutputRow`
    - src/features/allocation/domain/allocation-types.ts contains `roomLabel`
    - src/features/allocation/domain/allocation-types.ts contains `export interface AllocationHistoryItem`
    - src/features/allocation/domain/allocation-types.ts contains `export interface AllocationRetentionPolicy`
  </acceptance_criteria>
  <done>The codebase has explicit contracts for output rows, history metadata, and retention-policy responses instead of relying on ad-hoc object shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Build authoritative output projection and retention-aware history services</name>
  <files>src/features/allocation/server/project-output-records.ts, src/features/allocation/server/history-retention.ts, src/features/allocation/server/list-allocation-runs.ts, src/features/allocation/server/load-allocation-run.ts, tests/allocation/project-output-records.test.ts</files>
  <read_first>src/features/allocation/server/load-allocation-run.ts, src/features/allocation/domain/allocation-types.ts, src/features/allocation/ui/allocation-room-table.tsx, src/features/allocation/ui/allocation-result-summary.tsx, prisma/schema.prisma</read_first>
  <action>Create `project-output-records.ts` to flatten the authoritative saved run into stable rows ordered by `roomNumber` then `seatIndex`, with the exact output columns needed by workbook and print flows. Create `history-retention.ts` with `ALLOCATION_RUN_RETENTION_DAYS`, `getAllocationRetentionPolicy`, `isAllocationRunExpired`, and `pruneExpiredAllocationRuns`; compute age from `lastEditedAt ?? updatedAt ?? createdAt`. Create `list-allocation-runs.ts` to query active runs newest-first, map them into `AllocationHistoryItem`, and attach the current retention policy. Update `load-allocation-run.ts` so expired runs are pruned or rejected before returning an authoritative saved run. Add focused unit coverage proving output rows preserve room labels, candidate numbers, and student ordering from the authoritative preview.</action>
  <verify>npm run test -- tests/allocation/project-output-records.test.ts</verify>
  <acceptance_criteria>
    - src/features/allocation/server/project-output-records.ts contains `roomLabel`
    - src/features/allocation/server/project-output-records.ts contains `candidateNumber`
    - src/features/allocation/server/history-retention.ts contains `ALLOCATION_RUN_RETENTION_DAYS`
    - src/features/allocation/server/history-retention.ts contains `isAllocationRunExpired`
    - src/features/allocation/server/list-allocation-runs.ts contains `findMany`
    - tests/allocation/project-output-records.test.ts contains `Phòng thi`
    - tests/allocation/project-output-records.test.ts contains `candidateNumber`
  </acceptance_criteria>
  <done>Later output surfaces can reuse one server-owned row projection, and expired runs are handled consistently before any reopen or output action occurs.</done>
</task>

<task type="auto">
  <name>Task 3: Add saved-run history listing to the allocations API and verify retention behavior</name>
  <files>src/app/api/allocations/route.ts, tests/api/allocations-route.test.ts</files>
  <read_first>src/app/api/allocations/route.ts, src/features/allocation/server/list-allocation-runs.ts, src/features/allocation/server/history-retention.ts, tests/api/allocations-route.test.ts</read_first>
  <action>Extend `src/app/api/allocations/route.ts` with `GET` so the same endpoint that persists new runs can also return history metadata for reopening. `GET` must return the retention policy plus active runs sorted newest-first. Preserve the existing `POST` behavior unchanged. Extend `tests/api/allocations-route.test.ts` with cases for successful history listing, retention metadata in the response body, and expired runs being filtered or pruned before the API returns data.</action>
  <verify>npm run test -- tests/api/allocations-route.test.ts</verify>
  <acceptance_criteria>
    - src/app/api/allocations/route.ts contains `export async function GET`
    - src/app/api/allocations/route.ts contains `listAllocationRuns`
    - tests/api/allocations-route.test.ts contains `retention`
    - tests/api/allocations-route.test.ts contains `expired`
    - tests/api/allocations-route.test.ts contains `sourceFileName`
  </acceptance_criteria>
  <done>The homepage has a supported API surface for listing reopenable runs without weakening the existing create-run route.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test -- tests/allocation/project-output-records.test.ts tests/api/allocations-route.test.ts passes
- [ ] npm run typecheck passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Reopen/history flows have a retention-aware API surface
- Export and print flows can consume one authoritative output-row projection
</success_criteria>

<threat_model>
<trust_boundaries>
- Prisma `AllocationRun` rows -> history/reopen API payloads
- authoritative saved-run snapshot -> shared output projection
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>04-01-T1</threat_id>
    <category>Information Disclosure</category>
    <component>Saved-run history and reopen endpoints</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Apply retention checks before returning history rows or reopening a run. Expired runs must be pruned or treated as not found so public access cannot surface stale student data past policy limits.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>04-01-T2</threat_id>
    <category>Integrity</category>
    <component>Output projection foundation</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Create one server-side output-row projector from the authoritative saved run and require later export/print plans to consume it directly instead of re-deriving rows in separate code paths.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/04-output-operations/04-01-SUMMARY.md`
</output>
