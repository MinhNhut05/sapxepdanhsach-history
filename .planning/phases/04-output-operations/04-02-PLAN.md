---
phase: 04-output-operations
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/features/allocation/server/export-allocation-workbook.ts
  - src/app/api/allocations/[id]/export/route.ts
  - tests/allocation/export-allocation-workbook.test.ts
  - tests/api/allocation-export-route.test.ts
autonomous: true
requirements:
  - EXPT-01
  - EXPT-02
  - EXPT-03
  - EXPT-04
  - EXPT-06
user_setup: []
must_haves:
  truths:
    - "Workbook export is generated on the server from the authoritative saved run, never from client draft state."
    - "The workbook always contains one master sheet and one per-room sheet with the exact required output columns."
    - "Excel row ordering and candidate numbers match the authoritative preview exactly because the workbook consumes the shared output projection."
  artifacts:
    - path: src/features/allocation/server/export-allocation-workbook.ts
      provides: "Excel workbook generation with master and per-room sheets"
      contains: "Workbook"
    - path: src/app/api/allocations/[id]/export/route.ts
      provides: "Download route for `.xlsx` output"
      contains: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
    - path: tests/api/allocation-export-route.test.ts
      provides: "Binary route verification for workbook download"
      contains: "content-disposition"
  key_links:
    - from: src/features/allocation/server/export-allocation-workbook.ts
      to: src/features/allocation/server/project-output-records.ts
      via: "Workbook rows are built from the shared authoritative projection"
      pattern: "projectOutputRecords"
    - from: src/app/api/allocations/[id]/export/route.ts
      to: src/features/allocation/server/load-allocation-run.ts
      via: "The route exports the authoritative edited-or-original run rather than accepting client-sent payloads"
      pattern: "loadAllocationRun"
---

<objective>
Deliver the production workbook export path for Phase 4.

Purpose: Operators need a real `.xlsx` output that can be downloaded and used operationally, and the binary file must preserve the exact ordering and candidate numbers already approved in the authoritative preview.
Output: workbook generator, download route, and tests that read the generated file back to verify sheet structure and row parity.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-output-operations/04-RESEARCH.md
@.planning/phases/04-output-operations/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the authoritative workbook generator with exact sheet and column rules</name>
  <files>src/features/allocation/server/export-allocation-workbook.ts, tests/allocation/export-allocation-workbook.test.ts</files>
  <read_first>package.json, src/features/allocation/server/project-output-records.ts, src/features/allocation/server/load-allocation-run.ts, src/features/roster/server/read-workbook.ts, src/features/allocation/ui/allocation-room-table.tsx</read_first>
  <action>Create `export-allocation-workbook.ts` using `exceljs`. Generate one master sheet named `Tổng hợp` and one sheet per room named `Phòng XX` with `XX` zero-padded to 2 digits. Every sheet must use the exact column order `Phòng thi`, `Số báo danh`, `Thứ tự trong phòng`, `Lớp`, `MSHV`, `HỌ LÓT`, `TÊN`, `NGÀY SINH`, `NƠI SINH`, `GHI CHÚ`. Freeze the header row, render headers in bold, set readable widths for Vietnamese text columns, and populate rows from the shared authoritative output projection so workbook order follows `roomNumber` then `seatIndex`. Add unit coverage that loads the generated workbook bytes and asserts the master sheet, per-room sheets, column titles, and representative row values.</action>
  <verify>npm run test -- tests/allocation/export-allocation-workbook.test.ts</verify>
  <acceptance_criteria>
    - src/features/allocation/server/export-allocation-workbook.ts contains `Tổng hợp`
    - src/features/allocation/server/export-allocation-workbook.ts contains `Phòng thi`
    - src/features/allocation/server/export-allocation-workbook.ts contains `Số báo danh`
    - src/features/allocation/server/export-allocation-workbook.ts contains `Phòng ${`
    - tests/allocation/export-allocation-workbook.test.ts contains `Tổng hợp`
    - tests/allocation/export-allocation-workbook.test.ts contains `Phòng 01`
    - tests/allocation/export-allocation-workbook.test.ts contains `Số báo danh`
  </acceptance_criteria>
  <done>The codebase can generate the exact workbook structure required for final operational export.</done>
</task>

<task type="auto">
  <name>Task 2: Expose workbook download as a retention-aware export route</name>
  <files>src/app/api/allocations/[id]/export/route.ts, tests/api/allocation-export-route.test.ts</files>
  <read_first>src/app/api/allocations/[id]/route.ts, src/features/allocation/server/load-allocation-run.ts, src/features/allocation/server/export-allocation-workbook.ts, src/features/allocation/server/history-retention.ts</read_first>
  <action>Create `src/app/api/allocations/[id]/export/route.ts` with `GET` that loads the authoritative saved run server-side, calls the workbook generator, and returns workbook bytes with `content-type` set to `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` and `content-disposition` set to `attachment; filename=\"phan-phong-{id}.xlsx\"`. Reuse the same not-found/retention failure behavior as the saved-run load path. Add route tests for successful download, missing run handling, expired-run handling, and binary headers.</action>
  <verify>npm run test -- tests/api/allocation-export-route.test.ts</verify>
  <acceptance_criteria>
    - src/app/api/allocations/[id]/export/route.ts contains `export async function GET`
    - src/app/api/allocations/[id]/export/route.ts contains `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
    - src/app/api/allocations/[id]/export/route.ts contains `content-disposition`
    - tests/api/allocation-export-route.test.ts contains `phan-phong-`
    - tests/api/allocation-export-route.test.ts contains `not_found`
    - tests/api/allocation-export-route.test.ts contains `expired`
  </acceptance_criteria>
  <done>The application exposes a real download endpoint for the final workbook instead of leaving export as a client-only idea.</done>
</task>

<task type="auto">
  <name>Task 3: Prove workbook rows match the authoritative preview exactly</name>
  <files>tests/allocation/export-allocation-workbook.test.ts, tests/api/allocation-export-route.test.ts</files>
  <read_first>tests/allocation/export-allocation-workbook.test.ts, tests/api/allocation-export-route.test.ts, src/features/allocation/server/project-output-records.ts, src/features/allocation/server/export-allocation-workbook.ts</read_first>
  <action>Extend the workbook and route tests so they assert parity, not just file existence. Read the generated workbook back with `exceljs`, then verify that the master sheet and the room sheet for `Phòng 01` contain the same candidate numbers, room labels, and row ordering as the authoritative output projection used in the saved preview. Include at least one assertion that an edited saved run exports the edited order rather than the original auto-allocation order.</action>
  <verify>npm run test -- tests/allocation/export-allocation-workbook.test.ts tests/api/allocation-export-route.test.ts</verify>
  <acceptance_criteria>
    - tests/allocation/export-allocation-workbook.test.ts contains `edited`
    - tests/allocation/export-allocation-workbook.test.ts contains `candidateNumber`
    - tests/api/allocation-export-route.test.ts contains `Phòng 01`
    - tests/api/allocation-export-route.test.ts contains `edited`
  </acceptance_criteria>
  <done>The export path has regression coverage for the exact parity guarantee Phase 4 promises.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test -- tests/allocation/export-allocation-workbook.test.ts tests/api/allocation-export-route.test.ts passes
- [ ] npm run typecheck passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Operators can download a workbook with one master sheet and one sheet per room
- Workbook rows match the authoritative preview for room order and candidate numbers
</success_criteria>

<threat_model>
<trust_boundaries>
- authoritative saved run -> workbook generator
- export route response -> operator download target
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>04-02-T1</threat_id>
    <category>Integrity</category>
    <component>Workbook generation</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Build sheets from the shared authoritative output projection and add tests that re-open the produced workbook bytes to confirm row ordering and candidate numbers match the saved preview exactly.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>04-02-T2</threat_id>
    <category>Information Disclosure</category>
    <component>Export download route</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Load the saved run server-side and honor retention/not-found checks before returning workbook bytes so expired or missing runs do not leak student data through the export endpoint.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/04-output-operations/04-02-SUMMARY.md`
</output>
