---
phase: 04-output-operations
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/allocations/[id]/print/page.tsx
  - src/features/allocation/ui/print-room-view.tsx
  - src/features/allocation/ui/print-room-actions.tsx
  - src/app/globals.css
  - tests/ui/print-room-view.test.tsx
autonomous: true
requirements:
  - EXPT-05
  - EXPT-06
user_setup: []
must_haves:
  truths:
    - "Room printouts come from a dedicated web route instead of printing the full homepage workspace."
    - "Printed room rows use the same authoritative output projection as workbook export, so room order and candidate numbers cannot drift."
    - "Invalid or expired print requests fail cleanly instead of rendering a misleading page."
  artifacts:
    - path: src/app/allocations/[id]/print/page.tsx
      provides: "Room-specific print route for saved runs"
      contains: "searchParams"
    - path: src/features/allocation/ui/print-room-actions.tsx
      provides: "In-browser print trigger for the room page"
      contains: "window.print"
    - path: src/app/globals.css
      provides: "Print-specific layout rules"
      contains: "@media print"
  key_links:
    - from: src/app/allocations/[id]/print/page.tsx
      to: src/features/allocation/server/project-output-records.ts
      via: "The route renders room rows from the shared authoritative output projection"
      pattern: "projectOutputRecords"
    - from: src/features/allocation/ui/print-room-view.tsx
      to: src/features/allocation/ui/print-room-actions.tsx
      via: "The print page combines fixed room layout with a deliberate browser print action"
      pattern: "PrintRoomActions"
---

<objective>
Deliver the print-friendly room view for Phase 4.

Purpose: Operators need a room-specific list that can be printed directly from the web without dragging the entire homepage UI onto paper, and the printed content must match the approved preview exactly.
Output: dedicated print route, printable room component, print action, print CSS, and UI tests for room-level parity.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-output-operations/04-RESEARCH.md
@.planning/phases/04-output-operations/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the printable room layout and print trigger components</name>
  <files>src/features/allocation/ui/print-room-view.tsx, src/features/allocation/ui/print-room-actions.tsx, tests/ui/print-room-view.test.tsx</files>
  <read_first>src/features/allocation/ui/allocation-room-table.tsx, src/features/allocation/ui/allocation-result-summary.tsx, src/features/allocation/server/project-output-records.ts, src/app/globals.css</read_first>
  <action>Create `print-room-view.tsx` that renders one room's printable roster using the authoritative output projection. The visible columns must include `Phòng thi`, `Số báo danh`, `Thứ tự trong phòng`, `Lớp`, `MSHV`, `Họ tên`, `Ngày sinh`, and `Nơi sinh`. Add `print-room-actions.tsx` as a client component with a primary button labeled `In danh sách phòng này` that calls `window.print()`. Add UI coverage asserting the room title, candidate numbers, and print button render from the supplied authoritative rows.</action>
  <verify>npm run test -- tests/ui/print-room-view.test.tsx</verify>
  <acceptance_criteria>
    - src/features/allocation/ui/print-room-view.tsx contains `Số báo danh`
    - src/features/allocation/ui/print-room-view.tsx contains `Ngày sinh`
    - src/features/allocation/ui/print-room-actions.tsx contains `window.print`
    - src/features/allocation/ui/print-room-actions.tsx contains `In danh sách phòng này`
    - tests/ui/print-room-view.test.tsx contains `Phòng 1`
    - tests/ui/print-room-view.test.tsx contains `window.print`
  </acceptance_criteria>
  <done>The codebase has a dedicated printable room component and a direct browser print action instead of relying on the generic workspace DOM.</done>
</task>

<task type="auto">
  <name>Task 2: Add a room-specific print page that validates room selection and saved-run availability</name>
  <files>src/app/allocations/[id]/print/page.tsx</files>
  <read_first>src/app/api/allocations/[id]/route.ts, src/features/allocation/server/load-allocation-run.ts, src/features/allocation/server/project-output-records.ts, src/features/allocation/ui/print-room-view.tsx</read_first>
  <action>Create `src/app/allocations/[id]/print/page.tsx` as a server page that reads `searchParams.room`, validates that it is a positive integer room number present in the authoritative saved run, loads the saved run server-side, and renders the printable room view plus print actions. If the room query is missing, invalid, or not part of the saved run, render a clear not-found state instead of guessing a room. Preserve retention-aware failure behavior by reusing the saved-run load path.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/app/allocations/[id]/print/page.tsx contains `searchParams`
    - src/app/allocations/[id]/print/page.tsx contains `room`
    - src/app/allocations/[id]/print/page.tsx contains `notFound`
    - src/app/allocations/[id]/print/page.tsx contains `loadAllocationRun`
  </acceptance_criteria>
  <done>The app exposes one stable web route for printing a room list from an authoritative saved run.</done>
</task>

<task type="auto">
  <name>Task 3: Add print-specific CSS rules so only the room list goes to paper</name>
  <files>src/app/globals.css, tests/ui/print-room-view.test.tsx</files>
  <read_first>src/app/globals.css, src/features/allocation/ui/print-room-view.tsx, src/features/allocation/ui/print-room-actions.tsx</read_first>
  <action>Extend `src/app/globals.css` with `@media print` rules that hide interactive chrome, keep the page background white, preserve readable table borders, and avoid splitting the room header from the roster table. Ensure the room print page remains usable on screen while producing a clean paper layout. Add or extend tests so they assert print-specific class names or labels used by the printable view.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/app/globals.css contains `@media print`
    - src/app/globals.css contains `.print-only`
    - src/app/globals.css contains `.screen-only`
    - tests/ui/print-room-view.test.tsx contains `print`
  </acceptance_criteria>
  <done>The room print route produces a clean print layout instead of dumping the homepage interface onto paper.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test -- tests/ui/print-room-view.test.tsx passes
- [ ] npm run typecheck passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Operators can print a room-specific list directly from the web
- Printed room rows match the authoritative preview for room order and candidate numbers
</success_criteria>

<threat_model>
<trust_boundaries>
- print page query string -> saved-run room selection
- authoritative saved run -> rendered print HTML
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>04-03-T1</threat_id>
    <category>Integrity</category>
    <component>Print page room selection</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Validate `searchParams.room` against the authoritative saved run and fail closed for missing or invalid room numbers instead of rendering guessed content.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>04-03-T2</threat_id>
    <category>Integrity</category>
    <component>Printed roster layout</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Render the print page from the same shared output projection used by Excel export and add tests that assert room-specific candidate numbers and row ordering in the printable HTML.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/04-output-operations/04-03-SUMMARY.md`
</output>
