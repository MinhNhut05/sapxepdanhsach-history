---
phase: 04-output-operations
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - src/features/allocation/ui/allocation-history-panel.tsx
  - src/features/allocation/ui/allocation-output-actions.tsx
  - src/features/allocation/ui/allocation-workspace.tsx
  - tests/ui/allocation-operations-panel.test.tsx
  - tests/ui/allocation-workspace.test.tsx
autonomous: true
requirements:
  - EXPT-01
  - EXPT-05
  - HIST-02
  - SAFE-03
user_setup: []
must_haves:
  truths:
    - "Operators can reopen a saved run from history without re-importing a roster."
    - "The homepage surfaces real export and print actions only when an authoritative saved run is loaded."
    - "Retention policy messaging is visible in the operations UI so staff understand when saved runs may disappear."
  artifacts:
    - path: src/features/allocation/ui/allocation-history-panel.tsx
      provides: "Saved-run history UI with reopen actions"
      contains: "Mở lại"
    - path: src/features/allocation/ui/allocation-output-actions.tsx
      provides: "Workspace output links for workbook download and room print"
      contains: "Xuất Excel"
    - path: src/features/allocation/ui/allocation-workspace.tsx
      provides: "Integrated import/review/reopen/output control surface"
      contains: "handleReopenRun"
  key_links:
    - from: src/features/allocation/ui/allocation-history-panel.tsx
      to: src/app/api/allocations/route.ts
      via: "History panel consumes the retention-aware list endpoint for reopenable saved runs"
      pattern: "/api/allocations"
    - from: src/features/allocation/ui/allocation-output-actions.tsx
      to: src/app/api/allocations/[id]/export/route.ts
      via: "The workspace links output actions to the server-owned export and print routes"
      pattern: "/api/allocations/"
---

<objective>
Integrate Phase 4 operations into the homepage workspace.

Purpose: Export and print routes are not enough unless operators can reopen the right saved run and trigger those actions from the existing control surface.
Output: history panel, output-actions panel, workspace integration, and regression tests for reopen/export/print entrypoints.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-output-operations/04-RESEARCH.md
@.planning/phases/04-output-operations/04-01-PLAN.md
@.planning/phases/04-output-operations/04-02-PLAN.md
@.planning/phases/04-output-operations/04-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add saved-run history and retention messaging components to the homepage</name>
  <files>src/features/allocation/ui/allocation-history-panel.tsx, src/features/allocation/ui/allocation-workspace.tsx, tests/ui/allocation-operations-panel.test.tsx</files>
  <read_first>src/features/allocation/ui/allocation-workspace.tsx, src/features/allocation/ui/allocation-result-summary.tsx, src/features/allocation/domain/allocation-types.ts, src/app/globals.css</read_first>
  <action>Create `allocation-history-panel.tsx` to render history rows from `GET /api/allocations`, including `sourceFileName`, `createdAt`, `strategy`, `roomCount`, `totalStudents`, edited status, and visible retention-policy wording. Include a primary action labeled `Mở lại` for each row. Update `allocation-workspace.tsx` so the page fetches the history list, renders the new panel, and displays loading/error states without disrupting the existing import-to-allocation flow. Add component tests asserting history metadata, retention text, and the reopen button render correctly.</action>
  <verify>npm run test -- tests/ui/allocation-operations-panel.test.tsx</verify>
  <acceptance_criteria>
    - src/features/allocation/ui/allocation-history-panel.tsx contains `Mở lại`
    - src/features/allocation/ui/allocation-history-panel.tsx contains `retention`
    - src/features/allocation/ui/allocation-workspace.tsx contains `fetch('/api/allocations'`
    - tests/ui/allocation-operations-panel.test.tsx contains `Mở lại`
    - tests/ui/allocation-operations-panel.test.tsx contains `Xuất Excel`
  </acceptance_criteria>
  <done>The homepage exposes retained saved runs and explains the cleanup policy instead of requiring operators to know run IDs manually.</done>
</task>

<task type="auto">
  <name>Task 2: Add output action links for workbook download and room print</name>
  <files>src/features/allocation/ui/allocation-output-actions.tsx, src/features/allocation/ui/allocation-workspace.tsx, tests/ui/allocation-operations-panel.test.tsx</files>
  <read_first>src/features/allocation/ui/allocation-workspace.tsx, src/features/allocation/domain/allocation-types.ts, src/app/api/allocations/[id]/export/route.ts, src/app/allocations/[id]/print/page.tsx</read_first>
  <action>Create `allocation-output-actions.tsx` that accepts the currently loaded authoritative saved run and renders a download action labeled `Xuất Excel` pointing at `/api/allocations/{id}/export` plus one room-specific print link per room pointing at `/allocations/{id}/print?room={roomNumber}`. Update `allocation-workspace.tsx` so these actions appear only when `savedRun` exists. Preserve the authoritative-preview messaging already present in Phase 3 so operators can tell the actions use the current saved result. Extend tests to assert the exact export href and room print hrefs.</action>
  <verify>npm run test -- tests/ui/allocation-operations-panel.test.tsx</verify>
  <acceptance_criteria>
    - src/features/allocation/ui/allocation-output-actions.tsx contains `Xuất Excel`
    - src/features/allocation/ui/allocation-output-actions.tsx contains `/api/allocations/`
    - src/features/allocation/ui/allocation-output-actions.tsx contains `/print?room=`
    - src/features/allocation/ui/allocation-workspace.tsx contains `AllocationOutputActions`
    - tests/ui/allocation-operations-panel.test.tsx contains `/print?room=`
  </acceptance_criteria>
  <done>The homepage gives operators a direct path from authoritative saved run to export and print without extra manual URL construction.</done>
</task>

<task type="auto">
  <name>Task 3: Wire reopen flow into the workspace and verify end-to-end operations behavior</name>
  <files>src/features/allocation/ui/allocation-workspace.tsx, tests/ui/allocation-workspace.test.tsx</files>
  <read_first>src/features/allocation/ui/allocation-workspace.tsx, src/features/allocation/ui/allocation-history-panel.tsx, src/features/allocation/server/load-allocation-run.ts, tests/ui/allocation-workspace.test.tsx</read_first>
  <action>Update `allocation-workspace.tsx` with a `handleReopenRun` flow that fetches `/api/allocations/{id}`, hydrates `savedRun` from the authoritative response, exits edit mode, clears stale allocation/save errors, and keeps reopened-run review/output actions available even when no new roster has been imported. Extend `tests/ui/allocation-workspace.test.tsx` to cover history fetch, successful reopen of a saved run, retention-aware error feedback when reopen fails, and continued visibility of export/print actions after reopening.</action>
  <verify>npm run test -- tests/ui/allocation-workspace.test.tsx tests/ui/allocation-operations-panel.test.tsx</verify>
  <acceptance_criteria>
    - src/features/allocation/ui/allocation-workspace.tsx contains `async function handleReopenRun`
    - src/features/allocation/ui/allocation-workspace.tsx contains `/api/allocations/${`
    - src/features/allocation/ui/allocation-workspace.tsx contains `setIsEditMode(false)`
    - tests/ui/allocation-workspace.test.tsx contains `reopen`
    - tests/ui/allocation-workspace.test.tsx contains `Xuất Excel`
    - tests/ui/allocation-workspace.test.tsx contains `print?room=`
  </acceptance_criteria>
  <done>The existing homepage workspace can reopen prior saved runs and drive output operations from the authoritative result.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test -- tests/ui/allocation-operations-panel.test.tsx tests/ui/allocation-workspace.test.tsx passes
- [ ] npm run typecheck passes
- [ ] npm run build passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Operators can reopen a saved run from history and trigger export/print actions from the homepage
- Retention policy is visible and enforced in the operations UI
</success_criteria>

<threat_model>
<trust_boundaries>
- history API response -> homepage operations UI
- reopened saved run -> output action links
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>04-04-T1</threat_id>
    <category>Integrity</category>
    <component>Workspace reopen flow</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>When reopening a run, replace the workspace saved-run state with the full authoritative response, exit edit mode, and clear stale errors so the UI cannot mix a new history run with old import or draft state.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>04-04-T2</threat_id>
    <category>Information Disclosure</category>
    <component>Operations UI history surface</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Render retention policy messaging from the server response and rely on retention-aware list/reopen routes so expired runs disappear from the UI rather than remaining actionable.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/04-output-operations/04-04-SUMMARY.md`
</output>
