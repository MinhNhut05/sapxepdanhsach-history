---
phase: 05-smart-intake-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/features/roster/domain/intake-review.ts
  - src/features/roster/server/file-guard.ts
  - src/features/roster/server/read-intake-file.ts
  - src/features/roster/server/detect-csv-format.ts
  - src/features/roster/server/detect-header-row.ts
  - src/features/roster/lib/map-headers.ts
  - src/features/roster/ui/import-state.ts
  - tests/roster/map-headers.test.ts
  - tests/roster/read-intake-file.test.ts
autonomous: true
requirements:
  - INTK-01
  - INTK-02
  - REVW-01
user_setup: []
must_haves:
  truths:
    - "Phase 5 has one shared intake contract that can represent clean success, review-required intake, and hard failure without creating a second import workflow."
    - "The server can read `.xlsx`, `.xls`, and `.csv` into one normalized row snapshot before any canonical validation runs."
    - "Header detection is no longer hard-coded to row 1 because title rows, blank rows, and alias headers are treated as recoverable noise."
  artifacts:
    - path: src/features/roster/domain/intake-review.ts
      provides: "Shared review and audit contracts for Smart Intake"
      contains: "export interface IntakeReviewPayload"
    - path: src/features/roster/server/read-intake-file.ts
      provides: "Unified format reader for `.xlsx`, `.xls`, and `.csv`"
      contains: "export async function readIntakeFile"
    - path: src/features/roster/server/detect-header-row.ts
      provides: "Recoverable header-row detection"
      contains: "export function detectHeaderRow"
  key_links:
    - from: src/features/roster/server/read-intake-file.ts
      to: src/features/roster/server/detect-csv-format.ts
      via: "CSV bytes are normalized through shared delimiter and encoding heuristics before row extraction"
      pattern: "detectCsvFormat"
    - from: src/features/roster/server/detect-header-row.ts
      to: src/features/roster/lib/map-headers.ts
      via: "Header-row selection is scored by actual required and alias-header coverage"
      pattern: "mapRosterHeaders"
    - from: src/features/roster/ui/import-state.ts
      to: src/features/roster/domain/intake-review.ts
      via: "The UI consumes the same Smart Intake review contract that the server produces"
      pattern: "IntakeReviewPayload"
---

<objective>
Create the shared Phase 5 intake foundation: richer import contracts, multi-format file support, CSV sniffing, and tolerant header-row detection.

Purpose: The current import stack only accepts clean `.xlsx` files with the header on row 1, which cannot satisfy Smart Intake.
Output: one normalized intake reader plus shared review-state contracts that later plans can build on.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-smart-intake-core/05-CONTEXT.md
@.planning/phases/05-smart-intake-core/05-PRD.md
@.planning/phases/05-smart-intake-core/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Smart Intake contracts and expand upload guardrails to `.xlsx`, `.xls`, and `.csv`</name>
  <files>package.json, package-lock.json, src/features/roster/domain/intake-review.ts, src/features/roster/server/file-guard.ts, src/features/roster/ui/import-state.ts</files>
  <read_first>package.json, src/features/roster/server/file-guard.ts, src/features/roster/ui/import-state.ts, src/app/api/rosters/import/route.ts, .planning/phases/05-smart-intake-core/05-CONTEXT.md</read_first>
  <action>Add a parsing dependency that can read `.xls` and `.csv` in addition to `.xlsx` by installing `xlsx` in `package.json` and updating `package-lock.json`. Create `src/features/roster/domain/intake-review.ts` with the exact shared contracts `IntakeFlowState = "ready" | "review_required" | "failed"`, `IntakeConfidenceBand = "high" | "medium" | "low"`, `IntakeAuditRecord`, `IntakeReviewItem`, and `IntakeReviewPayload`. Extend `src/features/roster/ui/import-state.ts` so `ImportResultPayload` includes `intakeState`, `sourceFormat`, `requiresReview`, `fallbackUsed`, and optional `review`. Update `src/features/roster/server/file-guard.ts` to accept `.xlsx`, `.xls`, and `.csv` extensions, keep the 10 MB limit, and return a format hint such as `detectedExtension` so the server reader can branch without re-sniffing the filename.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - package.json contains `"xlsx"`
    - src/features/roster/domain/intake-review.ts contains `export type IntakeFlowState`
    - src/features/roster/domain/intake-review.ts contains `export interface IntakeAuditRecord`
    - src/features/roster/ui/import-state.ts contains `intakeState`
    - src/features/roster/ui/import-state.ts contains `fallbackUsed`
    - src/features/roster/server/file-guard.ts contains `.xls`
    - src/features/roster/server/file-guard.ts contains `.csv`
  </acceptance_criteria>
  <done>The repo has one Smart Intake contract and upload guard that recognizes all three required source formats.</done>
</task>

<task type="auto">
  <name>Task 2: Build unified workbook/CSV readers with deterministic delimiter and encoding detection</name>
  <files>src/features/roster/server/read-intake-file.ts, src/features/roster/server/detect-csv-format.ts, tests/roster/read-intake-file.test.ts</files>
  <read_first>src/features/roster/server/read-workbook.ts, src/features/roster/server/file-guard.ts, src/features/roster/ui/import-state.ts, tests/api/rosters-import-route.test.ts</read_first>
  <action>Create `src/features/roster/server/detect-csv-format.ts` with `detectCsvFormat` that scores the delimiters `,`, `;`, `\\t`, and `|` against the first meaningful lines and tries the encodings `utf-8`, `utf-16le`, `windows-1258`, and `windows-1252`. Create `src/features/roster/server/read-intake-file.ts` with `readIntakeFile(input, { fileName, mimeType })` that uses `xlsx` to read `.xlsx` and `.xls`, uses the CSV detector for `.csv`, and returns one `IntakeSheetSnapshot` shape containing `sourceFormat`, `sheetName`, `rows`, and `rowCount`. Add focused tests that prove `readIntakeFile` can extract rows from `.xlsx`, `.xls`, and `.csv`, and that CSV delimiter detection chooses the correct delimiter for comma-separated and semicolon-separated fixtures.</action>
  <verify>npm run test -- tests/roster/read-intake-file.test.ts</verify>
  <acceptance_criteria>
    - src/features/roster/server/detect-csv-format.ts contains `export function detectCsvFormat`
    - src/features/roster/server/detect-csv-format.ts contains `windows-1258`
    - src/features/roster/server/read-intake-file.ts contains `export async function readIntakeFile`
    - src/features/roster/server/read-intake-file.ts contains `sourceFormat`
    - tests/roster/read-intake-file.test.ts contains `.xls`
    - tests/roster/read-intake-file.test.ts contains `semicolon`
  </acceptance_criteria>
  <done>The server can normalize all required intake formats into one row snapshot before validation logic runs.</done>
</task>

<task type="auto">
  <name>Task 3: Add recoverable header-row detection and alias-header matching</name>
  <files>src/features/roster/server/detect-header-row.ts, src/features/roster/lib/map-headers.ts, tests/roster/map-headers.test.ts</files>
  <read_first>src/features/roster/lib/map-headers.ts, src/features/roster/server/read-workbook.ts, src/features/roster/server/row-validation.ts, .planning/phases/05-smart-intake-core/05-RESEARCH.md</read_first>
  <action>Create `src/features/roster/server/detect-header-row.ts` with `const MAX_HEADER_SCAN_ROWS = 8` and `detectHeaderRow(rows)` that scans the first 8 non-empty rows, skips title rows and blank rows, and selects the row with the best required-header coverage. Extend `src/features/roster/lib/map-headers.ts` to support a concrete alias dictionary: `Lớp|Lop|Tên lớp`, `MSHV|Mã HV|Ma hoc vien`, `HỌ LÓT|Họ lót|Ho lot`, `TÊN|Ten|First Name`, `NGÀY SINH|Ngày sinh|Ngay sinh|DOB`, `NƠI SINH|Nơi sinh|Noi sinh`, and `GHI CHÚ|Ghi chú|Ghi chu|Note`. Keep required-header failures explicit when the alias map still cannot recover the needed columns. Add tests proving a title row above the header is tolerated and that alias labels resolve to the expected canonical columns.</action>
  <verify>npm run test -- tests/roster/map-headers.test.ts</verify>
  <acceptance_criteria>
    - src/features/roster/server/detect-header-row.ts contains `export function detectHeaderRow`
    - src/features/roster/server/detect-header-row.ts contains `MAX_HEADER_SCAN_ROWS = 8`
    - src/features/roster/lib/map-headers.ts contains `Tên lớp`
    - src/features/roster/lib/map-headers.ts contains `Mã HV`
    - src/features/roster/lib/map-headers.ts contains `DOB`
    - tests/roster/map-headers.test.ts contains `title row`
    - tests/roster/map-headers.test.ts contains `alias`
  </acceptance_criteria>
  <done>Header discovery is tolerant enough for noisy spreadsheets without weakening required-column guarantees.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test -- tests/roster/map-headers.test.ts tests/roster/read-intake-file.test.ts passes
- [ ] npm run typecheck passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The intake stack can read `.xlsx`, `.xls`, and `.csv`
- Shared contracts now distinguish ready, review-required, and failed intake states
</success_criteria>

<threat_model>
<trust_boundaries>
- uploaded spreadsheet or CSV bytes -> server-side format readers
- detected rows and headers -> shared Smart Intake contract returned to the UI
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>05-01-T1</threat_id>
    <category>Integrity</category>
    <component>Header-row detection and alias mapping</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use deterministic header coverage scoring across the first 8 meaningful rows, keep required-header failures explicit when confidence is insufficient, and add regression tests for title-row and alias-header scenarios.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>05-01-T2</threat_id>
    <category>Availability</category>
    <component>Multi-format intake readers</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Keep the existing upload-size guard, limit CSV sniffing to a bounded preview window, and fail cleanly on unsupported or unreadable files instead of attempting unbounded parsing work.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/05-smart-intake-core/05-01-SUMMARY.md`
</output>
