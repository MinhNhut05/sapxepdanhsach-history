---
phase: 05-smart-intake-core
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/features/roster/server/build-intake-review.ts
  - src/features/roster/server/import-roster.ts
  - src/features/roster/server/row-validation.ts
  - src/features/roster/lib/parse-birth-date.ts
  - src/app/api/rosters/import/route.ts
  - tests/roster/import-roster.test.ts
  - tests/api/rosters-import-route.test.ts
autonomous: true
requirements:
  - INTK-03
  - INTK-04
  - REVW-01
  - AUDT-01
  - SAFE-04
user_setup: []
must_haves:
  truths:
    - "Recoverable noisy files return a structured review-required result instead of collapsing into the old all-or-nothing failure path."
    - "Only high-confidence safe fixes auto-apply; `MSHV` and `Lớp` always remain review-required even if the parser has a preferred proposal."
    - "Every machine-generated change carries audit metadata with raw value, proposed value, source, confidence, and reasoning."
  artifacts:
    - path: src/features/roster/server/build-intake-review.ts
      provides: "Review payload and audit-trail assembly for Smart Intake"
      contains: "auditTrail"
    - path: src/features/roster/server/import-roster.ts
      provides: "Rule-based orchestration for ready vs review-required intake"
      contains: "review_required"
    - path: src/app/api/rosters/import/route.ts
      provides: "Route semantics for recoverable intake responses"
      contains: "fallbackUsed"
  key_links:
    - from: src/features/roster/server/import-roster.ts
      to: src/features/roster/server/read-intake-file.ts
      via: "The Smart Intake orchestrator consumes normalized row snapshots instead of assuming `.xlsx` row 1 headers"
      pattern: "readIntakeFile"
    - from: src/features/roster/server/import-roster.ts
      to: src/features/roster/server/build-intake-review.ts
      via: "Recoverable mappings and repairs are converted into review items and audit records through one shared builder"
      pattern: "buildIntakeReview"
    - from: src/features/roster/server/row-validation.ts
      to: src/features/roster/lib/parse-birth-date.ts
      via: "Safe date normalization uses the expanded supported-format parser before confidence classification"
      pattern: "parseBirthDateValue"
---

<objective>
Turn the tolerant Phase 5 reader foundation into a rule-based Smart Intake pipeline that can auto-finish clean files, route noisy files into review, and attach a usable audit trail.

Purpose: Operators need recoverable intake, not just better parsing. The server must decide when to continue automatically and when to stop for review.
Output: a richer import orchestrator plus API responses that distinguish ready vs review-required results without breaking the existing entrypoint.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-smart-intake-core/05-CONTEXT.md
@.planning/phases/05-smart-intake-core/05-PRD.md
@.planning/phases/05-smart-intake-core/05-RESEARCH.md
@.planning/phases/05-smart-intake-core/05-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild import orchestration around tolerant file reading and recoverable validation</name>
  <files>src/features/roster/server/import-roster.ts, src/features/roster/server/row-validation.ts, src/features/roster/lib/parse-birth-date.ts</files>
  <read_first>src/features/roster/server/import-roster.ts, src/features/roster/server/row-validation.ts, src/features/roster/lib/parse-birth-date.ts, src/features/roster/server/read-intake-file.ts, src/features/roster/server/detect-header-row.ts, src/features/roster/domain/intake-review.ts</read_first>
  <action>Update `src/features/roster/server/import-roster.ts` to consume `readIntakeFile`, run `detectHeaderRow`, and validate the detected data rows instead of assuming the first worksheet header is always row 1. Extend `src/features/roster/lib/parse-birth-date.ts` to accept dotted day-first formats `dd.MM.yyyy` and `d.M.yyyy` as safe normalization inputs. Update `src/features/roster/server/row-validation.ts` to define `SAFE_AUTO_APPLY_REPAIR_TYPES = ["whitespace_cleanup", "casing_normalization", "birth_date_cleanup", "birth_place_cleanup", "note_cleanup", "header_alias_mapping"]`. Keep hard failures for unrecoverable structure, missing required values, and invalid birth dates that still cannot be normalized.</action>
  <verify>npm run test -- tests/roster/import-roster.test.ts</verify>
  <acceptance_criteria>
    - src/features/roster/server/import-roster.ts contains `readIntakeFile`
    - src/features/roster/server/import-roster.ts contains `detectHeaderRow`
    - src/features/roster/lib/parse-birth-date.ts contains `dd.MM.yyyy`
    - src/features/roster/server/row-validation.ts contains `birth_place_cleanup`
    - tests/roster/import-roster.test.ts contains `.csv`
    - tests/roster/import-roster.test.ts contains `title row`
  </acceptance_criteria>
  <done>The import service can recognize and normalize recoverable noisy files before it decides whether the operator must review them.</done>
</task>

<task type="auto">
  <name>Task 2: Create review-required payloads with confidence bands and audit data</name>
  <files>src/features/roster/server/build-intake-review.ts, src/features/roster/server/import-roster.ts, src/features/roster/server/row-validation.ts</files>
  <read_first>src/features/roster/domain/intake-review.ts, src/features/roster/server/import-roster.ts, src/features/roster/server/row-validation.ts, .planning/phases/05-smart-intake-core/05-CONTEXT.md</read_first>
  <action>Create `src/features/roster/server/build-intake-review.ts` with `buildIntakeReview` that emits the exact review shape `confidenceSummary`, `unresolvedCount`, `items`, `auditTrail`, and `stagedStudents`. Update `src/features/roster/server/import-roster.ts` so clean files return `{ ok: true, intakeState: "ready", requiresReview: false }`, recoverable noisy files return `{ ok: true, intakeState: "review_required", requiresReview: true }`, and only unrecoverable files return `{ ok: false, intakeState: "failed" }`. Enforce `SAFE-04` in code by marking any `className` or `studentCode` proposal as `requiresReview: true` and `autoApplied: false` even when the source is `rule` and the confidence is `high`.</action>
  <verify>npm run test -- tests/roster/import-roster.test.ts tests/api/rosters-import-route.test.ts</verify>
  <acceptance_criteria>
    - src/features/roster/server/build-intake-review.ts contains `export function buildIntakeReview`
    - src/features/roster/server/build-intake-review.ts contains `auditTrail`
    - src/features/roster/server/build-intake-review.ts contains `stagedStudents`
    - src/features/roster/server/import-roster.ts contains `intakeState: "review_required"`
    - src/features/roster/server/import-roster.ts contains `autoApplied: false`
    - src/features/roster/server/import-roster.ts contains `studentCode`
    - src/features/roster/server/import-roster.ts contains `className`
  </acceptance_criteria>
  <done>Recoverable imports now carry the audit and confidence data the review UI needs, while sensitive fields stay review-only.</done>
</task>

<task type="auto">
  <name>Task 3: Update the import route to return recoverable review responses and cover them with route tests</name>
  <files>src/app/api/rosters/import/route.ts, tests/api/rosters-import-route.test.ts</files>
  <read_first>src/app/api/rosters/import/route.ts, src/features/roster/server/import-roster.ts, src/features/roster/server/file-guard.ts, tests/api/rosters-import-route.test.ts</read_first>
  <action>Update `src/app/api/rosters/import/route.ts` so ready and review-required intake results both return HTTP `200`, while unrecoverable intake still returns `422` and upload/file-guard failures preserve `400`, `413`, and `415`. Include `sourceFileName`, `sourceFormat`, and `fallbackUsed` in the route payload. Extend `tests/api/rosters-import-route.test.ts` with cases for a clean `.xlsx` fast path, a recoverable noisy `.csv` review path, and a sensitive-field repair that remains review-required instead of auto-applying.</action>
  <verify>npm run test -- tests/api/rosters-import-route.test.ts</verify>
  <acceptance_criteria>
    - src/app/api/rosters/import/route.ts contains `status: importResult.ok ? 200 : 422`
    - src/app/api/rosters/import/route.ts contains `fallbackUsed`
    - tests/api/rosters-import-route.test.ts contains `review_required`
    - tests/api/rosters-import-route.test.ts contains `sourceFormat`
    - tests/api/rosters-import-route.test.ts contains `MSHV`
  </acceptance_criteria>
  <done>The existing import route can now surface recoverable review work without pretending it is a terminal validation error.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test -- tests/roster/import-roster.test.ts tests/api/rosters-import-route.test.ts passes
- [ ] npm run typecheck passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Recoverable noisy imports return a review-required payload instead of a hard failure
- Every machine-generated change includes audit metadata
- Sensitive fields stay review-only even when the server has a preferred proposal
</success_criteria>

<threat_model>
<trust_boundaries>
- normalized intake rows -> rule-based repair and confidence classification
- server-produced review payload -> operator review workspace in the browser
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>05-02-T1</threat_id>
    <category>Integrity</category>
    <component>Auto-applied safe repairs</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Encode a strict allowlist of safe auto-apply categories, force `MSHV` and `Lớp` proposals into review-required items, and verify the behavior with import and route tests.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>05-02-T2</threat_id>
    <category>Information Disclosure</category>
    <component>Audit-trail payloads</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Limit audit fields to the exact Smart Intake review needs, keep the payload tied to the current upload response, and avoid persisting pre-allocation intake artifacts in this phase.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/05-smart-intake-core/05-02-SUMMARY.md`
</output>
