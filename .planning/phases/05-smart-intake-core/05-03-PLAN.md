---
phase: 05-smart-intake-core
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/features/roster/ui/upload-panel.tsx
  - src/features/roster/ui/intake-review-panel.tsx
  - src/features/allocation/ui/allocation-workspace.tsx
  - src/features/allocation/ui/allocation-warning-panel.tsx
  - tests/ui/upload-panel.test.tsx
  - tests/ui/allocation-workspace.test.tsx
autonomous: true
requirements:
  - REVW-02
  - REVW-03
  - AUDT-01
  - SAFE-04
user_setup: []
must_haves:
  truths:
    - "Clean files keep the existing fast path, but review-required intake results stay inside the homepage workspace until the operator resolves them."
    - "Allocation controls remain disabled until the intake state becomes `ready`; review-required intake cannot leak unconfirmed repairs downstream."
    - "Operators can inspect confidence bands, audit details, and sensitive-field proposals from one review surface before continuing."
  artifacts:
    - path: src/features/roster/ui/intake-review-panel.tsx
      provides: "Operator review workspace for Smart Intake repairs"
      contains: "Tiếp tục sang phân phòng"
    - path: src/features/allocation/ui/allocation-workspace.tsx
      provides: "Allocation gating based on Smart Intake readiness"
      contains: "intakeState !== \"ready\""
    - path: src/features/roster/ui/upload-panel.tsx
      provides: "Updated upload entry point for `.xlsx`, `.xls`, and `.csv`"
      contains: "accept=\".xlsx,.xls,.csv\""
  key_links:
    - from: src/features/roster/ui/upload-panel.tsx
      to: src/features/roster/ui/import-state.ts
      via: "The upload surface renders ready, review-required, and failed states from one shared payload contract"
      pattern: "intakeState"
    - from: src/features/roster/ui/intake-review-panel.tsx
      to: src/features/allocation/ui/allocation-workspace.tsx
      via: "The review panel promotes confirmed staged students into the ready allocation flow only after unresolved items are cleared"
      pattern: "onResolved"
    - from: src/features/allocation/ui/allocation-workspace.tsx
      to: src/features/allocation/ui/allocation-warning-panel.tsx
      via: "Review-required intake warnings remain visible through the same warning surface used elsewhere in the workspace"
      pattern: "AllocationWarningPanel"
---

<objective>
Add the operator-facing Smart Intake review workspace and wire allocation gating to the new intake states.

Purpose: Recoverable intake is only useful if the UI can surface it safely. Operators must see what changed, approve sensitive proposals, and continue into allocation only when the intake result is ready.
Output: a review panel inside the existing homepage flow plus gating logic that preserves the clean fast path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-smart-intake-core/05-CONTEXT.md
@.planning/phases/05-smart-intake-core/05-PRD.md
@.planning/phases/05-smart-intake-core/05-RESEARCH.md
@.planning/phases/05-smart-intake-core/05-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the upload surface to understand ready, review-required, and failed Smart Intake states</name>
  <files>src/features/roster/ui/upload-panel.tsx, tests/ui/upload-panel.test.tsx</files>
  <read_first>src/features/roster/ui/upload-panel.tsx, src/features/roster/ui/import-state.ts, src/features/roster/ui/import-summary.tsx, tests/ui/upload-panel.test.tsx</read_first>
  <action>Update `src/features/roster/ui/upload-panel.tsx` so the file input accepts `.xlsx,.xls,.csv`, the status badges distinguish `ready`, `review_required`, and `failed`, and review-required payloads are not rendered as generic errors. Preserve the current preview table for ready results, keep blocking validation feedback for failed results, and pass review-required payloads through `onImportResultChange` so the parent workspace can open the review surface. Extend `tests/ui/upload-panel.test.tsx` with cases proving a review-required payload renders review messaging instead of the old error-only path.</action>
  <verify>npm run test -- tests/ui/upload-panel.test.tsx</verify>
  <acceptance_criteria>
    - src/features/roster/ui/upload-panel.tsx contains `accept=".xlsx,.xls,.csv"`
    - src/features/roster/ui/upload-panel.tsx contains `review_required`
    - src/features/roster/ui/upload-panel.tsx contains `intakeState`
    - tests/ui/upload-panel.test.tsx contains `review_required`
    - tests/ui/upload-panel.test.tsx contains `.csv`
  </acceptance_criteria>
  <done>The upload surface can now hand a recoverable intake result into the workspace instead of flattening it into a generic failure.</done>
</task>

<task type="auto">
  <name>Task 2: Build the intake review panel with confidence groups, audit details, and operator confirmation controls</name>
  <files>src/features/roster/ui/intake-review-panel.tsx, src/features/allocation/ui/allocation-warning-panel.tsx</files>
  <read_first>src/features/allocation/ui/allocation-warning-panel.tsx, src/features/roster/ui/import-state.ts, src/features/roster/domain/intake-review.ts, .planning/phases/05-smart-intake-core/05-CONTEXT.md</read_first>
  <action>Create `src/features/roster/ui/intake-review-panel.tsx` that groups `review.items` by `high`, `medium`, and `low` confidence, shows the exact audit fields `rawValue`, `proposedValue`, `source`, `confidence`, and `reasoning`, and forces manual confirmation for any `studentCode` or `className` proposal. The panel must expose a primary action labeled `Tiếp tục sang phân phòng` with `disabled={review.unresolvedCount > 0}`; when enabled and clicked, it emits a confirmed import payload with `intakeState: "ready"` and the approved staged students. Update `src/features/allocation/ui/allocation-warning-panel.tsx` only as needed so it can render Smart Intake review issues alongside existing warnings without losing the current allocation use cases.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/features/roster/ui/intake-review-panel.tsx contains `Tiếp tục sang phân phòng`
    - src/features/roster/ui/intake-review-panel.tsx contains `disabled={review.unresolvedCount > 0}`
    - src/features/roster/ui/intake-review-panel.tsx contains `rawValue`
    - src/features/roster/ui/intake-review-panel.tsx contains `proposedValue`
    - src/features/roster/ui/intake-review-panel.tsx contains `studentCode`
    - src/features/allocation/ui/allocation-warning-panel.tsx contains `info`
  </acceptance_criteria>
  <done>Operators have a dedicated review surface that explains what changed and prevents unresolved intake repairs from slipping through.</done>
</task>

<task type="auto">
  <name>Task 3: Gate allocation on Smart Intake readiness and verify the review flow in workspace tests</name>
  <files>src/features/allocation/ui/allocation-workspace.tsx, tests/ui/allocation-workspace.test.tsx</files>
  <read_first>src/features/allocation/ui/allocation-workspace.tsx, src/features/roster/ui/upload-panel.tsx, src/features/roster/ui/import-state.ts, tests/ui/allocation-workspace.test.tsx</read_first>
  <action>Update `src/features/allocation/ui/allocation-workspace.tsx` so `allocationDisabled` stays true whenever `importResult` is missing or `importResult.intakeState !== "ready"`. When the import result is `review_required`, render the new `IntakeReviewPanel` instead of the clean import preview, keep any review warnings visible, and only replace `importResult` with the confirmed ready payload after the operator resolves the review. Extend `tests/ui/allocation-workspace.test.tsx` with cases proving the allocation button is disabled during review, becomes enabled after confirmation, and remains unchanged for the clean fast path.</action>
  <verify>npm run test -- tests/ui/allocation-workspace.test.tsx</verify>
  <acceptance_criteria>
    - src/features/allocation/ui/allocation-workspace.tsx contains `importResult.intakeState !== "ready"`
    - src/features/allocation/ui/allocation-workspace.tsx contains `IntakeReviewPanel`
    - tests/ui/allocation-workspace.test.tsx contains `review_required`
    - tests/ui/allocation-workspace.test.tsx contains `disabled`
    - tests/ui/allocation-workspace.test.tsx contains `Tiếp tục sang phân phòng`
  </acceptance_criteria>
  <done>The homepage preserves the clean-file flow while safely stopping messy files in review until the operator clears them.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test -- tests/ui/upload-panel.test.tsx tests/ui/allocation-workspace.test.tsx passes
- [ ] npm run typecheck passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Review-required intake results render inside the homepage workspace
- Allocation stays disabled until the intake result is ready
- Audit details and sensitive-field proposals are visible before continuation
</success_criteria>

<threat_model>
<trust_boundaries>
- server-produced review payload -> operator confirmation controls in the browser
- confirmed Smart Intake result -> existing allocation workflow
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>05-03-T1</threat_id>
    <category>Integrity</category>
    <component>Review-to-allocation transition</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Keep allocation disabled until `intakeState` becomes `ready`, require unresolved review items to reach zero before the continue action enables, and cover the gating behavior in workspace tests.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>05-03-T2</threat_id>
    <category>Spoofing</category>
    <component>Sensitive-field review UI</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Render sensitive proposals with explicit confirmation controls and preserve visible audit details so operators cannot mistake an auto-applied suggestion for an original imported value.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/05-smart-intake-core/05-03-SUMMARY.md`
</output>
