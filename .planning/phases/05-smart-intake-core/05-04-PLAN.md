---
phase: 05-smart-intake-core
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - src/features/roster/server/intake-config.ts
  - src/features/roster/server/intake-ai-provider.ts
  - src/features/roster/server/intake-ai-fallback.ts
  - src/features/roster/server/import-roster.ts
  - tests/roster/import-roster.test.ts
  - tests/api/rosters-import-route.test.ts
  - tests/ui/allocation-workspace.test.tsx
autonomous: true
requirements:
  - REVW-01
  - AUDT-01
  - SAFE-05
user_setup: []
must_haves:
  truths:
    - "AI assistance is optional and provider-agnostic; the intake flow still works when no provider is configured or when the provider fails."
    - "AI suggestions never bypass the same safety rules that govern rule-based repairs, especially for `MSHV` and `Lớp`."
    - "The final Phase 5 regression suite proves fallback-to-review behavior rather than only the happy-path automation case."
  artifacts:
    - path: src/features/roster/server/intake-config.ts
      provides: "Server-only Smart Intake AI configuration parsing"
      contains: "SMART_INTAKE_AI_API_KEY"
    - path: src/features/roster/server/intake-ai-provider.ts
      provides: "Provider-agnostic AI interface for mapping and repair assistance"
      contains: "suggestRepairs"
    - path: src/features/roster/server/import-roster.ts
      provides: "Fallback-aware Smart Intake orchestration"
      contains: "fallbackUsed: true"
  key_links:
    - from: src/features/roster/server/import-roster.ts
      to: src/features/roster/server/intake-ai-provider.ts
      via: "The orchestrator only asks the provider to help with ambiguous mappings and repairs through a narrow interface"
      pattern: "suggestRepairs"
    - from: src/features/roster/server/import-roster.ts
      to: src/features/roster/server/intake-ai-fallback.ts
      via: "Provider failures degrade into rule-based review instead of aborting the intake flow"
      pattern: "handleIntakeAiFailure"
    - from: src/features/roster/server/intake-config.ts
      to: src/features/roster/server/intake-ai-provider.ts
      via: "Only validated server-side config reaches the provider implementation"
      pattern: "getSmartIntakeConfig"
---

<objective>
Add provider-agnostic AI assistance and harden the fallback path so Smart Intake reduces manual work without becoming dependent on an external provider.

Purpose: The core requirement is not “use AI”; it is “keep the operator moving even when AI is unavailable.”
Output: server-only AI configuration and provider abstraction, merged audit/source metadata, and regression coverage for provider failure scenarios.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-smart-intake-core/05-CONTEXT.md
@.planning/phases/05-smart-intake-core/05-PRD.md
@.planning/phases/05-smart-intake-core/05-RESEARCH.md
@.planning/phases/05-smart-intake-core/05-02-PLAN.md
@.planning/phases/05-smart-intake-core/05-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add server-only Smart Intake AI configuration and a provider-agnostic interface</name>
  <files>src/features/roster/server/intake-config.ts, src/features/roster/server/intake-ai-provider.ts</files>
  <read_first>package.json, src/features/roster/server/import-roster.ts, src/features/roster/domain/intake-review.ts, .planning/phases/05-smart-intake-core/05-CONTEXT.md</read_first>
  <action>Create `src/features/roster/server/intake-config.ts` with a `zod`-validated `getSmartIntakeConfig` helper that reads the exact server env keys `SMART_INTAKE_AI_PROVIDER`, `SMART_INTAKE_AI_BASE_URL`, `SMART_INTAKE_AI_API_KEY`, `SMART_INTAKE_AI_FAST_MODEL`, `SMART_INTAKE_AI_REASONING_MODEL`, and `SMART_INTAKE_AI_TIMEOUT_MS`. Create `src/features/roster/server/intake-ai-provider.ts` with a narrow `IntakeAiProvider` interface exposing `suggestHeaderMapping` and `suggestRepairs`, and keep the transport server-only by using `fetch` rather than any client-side SDK wiring.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - src/features/roster/server/intake-config.ts contains `SMART_INTAKE_AI_PROVIDER`
    - src/features/roster/server/intake-config.ts contains `SMART_INTAKE_AI_API_KEY`
    - src/features/roster/server/intake-ai-provider.ts contains `export interface IntakeAiProvider`
    - src/features/roster/server/intake-ai-provider.ts contains `suggestHeaderMapping`
    - src/features/roster/server/intake-ai-provider.ts contains `suggestRepairs`
  </acceptance_criteria>
  <done>The Smart Intake AI surface is explicit, server-only, and not locked to one provider implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate optional AI suggestions while enforcing the same safety policy and fallback behavior</name>
  <files>src/features/roster/server/intake-ai-fallback.ts, src/features/roster/server/import-roster.ts</files>
  <read_first>src/features/roster/server/import-roster.ts, src/features/roster/server/build-intake-review.ts, src/features/roster/server/intake-config.ts, src/features/roster/server/intake-ai-provider.ts, .planning/phases/05-smart-intake-core/05-RESEARCH.md</read_first>
  <action>Create `src/features/roster/server/intake-ai-fallback.ts` with `handleIntakeAiFailure` that normalizes timeout, quota, configuration, and generic provider failures into a rule-based fallback signal. Update `src/features/roster/server/import-roster.ts` so AI is only consulted when rule-based parsing still has ambiguous header mappings or medium/low-confidence repairs. Merge AI suggestions into the existing review/audit model with `source: "ai"` and explicit reasoning, then re-run the same safety policy that prevents silent auto-apply on `studentCode` and `className`. When the provider is missing, times out, or throws, return a usable rule-based result with `fallbackUsed: true` instead of failing intake.</action>
  <verify>npm run test -- tests/roster/import-roster.test.ts tests/api/rosters-import-route.test.ts</verify>
  <acceptance_criteria>
    - src/features/roster/server/intake-ai-fallback.ts contains `export function handleIntakeAiFailure`
    - src/features/roster/server/import-roster.ts contains `source: "ai"`
    - src/features/roster/server/import-roster.ts contains `fallbackUsed: true`
    - src/features/roster/server/import-roster.ts contains `studentCode`
    - src/features/roster/server/import-roster.ts contains `className`
  </acceptance_criteria>
  <done>AI can reduce manual review work, but provider failure only reduces automation and never blocks the operator.</done>
</task>

<task type="auto">
  <name>Task 3: Lock fallback behavior and review integration with regression coverage</name>
  <files>tests/roster/import-roster.test.ts, tests/api/rosters-import-route.test.ts, tests/ui/allocation-workspace.test.tsx</files>
  <read_first>tests/roster/import-roster.test.ts, tests/api/rosters-import-route.test.ts, tests/ui/allocation-workspace.test.tsx, src/features/roster/server/import-roster.ts, src/features/allocation/ui/allocation-workspace.tsx</read_first>
  <action>Extend the regression suite to cover three exact scenarios: an ambiguous mapping that uses AI and remains review-visible, a provider failure or quota exhaustion that still returns `review_required` with `fallbackUsed: true`, and a workspace flow where the operator can continue after fallback review but not before. Use stubs or test doubles for the provider interface so tests do not depend on network access.</action>
  <verify>npm run test -- tests/roster/import-roster.test.ts tests/api/rosters-import-route.test.ts tests/ui/allocation-workspace.test.tsx</verify>
  <acceptance_criteria>
    - tests/roster/import-roster.test.ts contains `fallbackUsed`
    - tests/roster/import-roster.test.ts contains `quota`
    - tests/api/rosters-import-route.test.ts contains `review_required`
    - tests/api/rosters-import-route.test.ts contains `fallbackUsed`
    - tests/ui/allocation-workspace.test.tsx contains `fallbackUsed`
  </acceptance_criteria>
  <done>The phase now proves the required degrade-gracefully behavior instead of only documenting it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test -- tests/roster/import-roster.test.ts tests/api/rosters-import-route.test.ts tests/ui/allocation-workspace.test.tsx passes
- [ ] npm run typecheck passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- AI configuration is server-only and provider-agnostic
- Provider failure returns rule-based review with `fallbackUsed: true`
- Sensitive-field safety rules still hold after AI suggestions are merged
</success_criteria>

<threat_model>
<trust_boundaries>
- server-only env config -> optional AI provider calls
- AI suggestion payloads -> Smart Intake review and audit model
</trust_boundaries>
<stride_register>
  <threat>
    <threat_id>05-04-T1</threat_id>
    <category>Information Disclosure</category>
    <component>AI provider configuration and request payloads</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Keep all provider secrets in server-only env parsing, avoid client-side SDK wiring, and send only the minimum repair context needed for mapping assistance.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>05-04-T2</threat_id>
    <category>Availability</category>
    <component>External AI dependency</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Wrap provider failures in a shared fallback helper, return rule-based review payloads with `fallbackUsed: true`, and cover timeout and quota scenarios in regression tests.</mitigation_plan>
  </threat>
</stride_register>
</threat_model>

<output>
After completion, create `.planning/phases/05-smart-intake-core/05-04-SUMMARY.md`
</output>
